import { lesson, q } from './helpers.mjs'

export const programmingSections = [
  lesson(
    'Clean Code Principles',
    [
      'Clean code is code that reads like well-written prose. Robert Martin and other software craftsmen emphasize that code is read far more often than it is written, so clarity beats cleverness. Meaningful names for variables, functions, and classes reveal intent without requiring comments to explain what a cryptic abbreviation means. A function named calculateMonthlyPayment communicates purpose immediately; a function named calc2 does not.',
      'Functions should do one thing and do it well. When a function mixes validation, database access, email sending, and response formatting, it becomes hard to test, hard to reuse, and easy to break when requirements change. Small functions with a single level of abstraction make call stacks easier to follow. If you need a comment to separate sections inside a function, that is often a signal to extract another function.',
      'Avoid duplication through the DRY principle: Don\'t Repeat Yourself. Copy-pasted logic diverges over time as one copy gets fixed and another does not. Extract shared behavior into functions, modules, or classes. At the same time, avoid premature abstraction—two similar blocks that change for different reasons should not be forced into one generic helper that becomes harder to understand than the duplication it replaced.',
      'Error handling should be explicit and consistent. Silent failures, empty catch blocks, and returning null without documentation force callers to guess what went wrong. Prefer throwing or returning typed errors that callers can handle deliberately. Guard clauses at the top of functions reduce nesting by returning early when preconditions fail, keeping the happy path readable at the bottom of the function.',
      'Formatting and consistency matter because they reduce cognitive load. Teams adopt linters and formatters so debates about brace style do not consume review time. Consistent indentation, line length, and import ordering let reviewers focus on logic rather than style. Code reviews become faster when every file in the repository follows the same visual rhythm.',
      'Clean code is not perfection—it is continuous improvement. Leave code slightly better than you found it when you touch a file. Refactor in small steps backed by tests. The goal is a codebase where new team members can navigate confidently, bugs are localized, and changes do not require archaeology through layers of accidental complexity.',
    ],
    [
      q(
        'What is the primary reason clean code favors meaningful names?',
        ['Names reveal intent and reduce the need for explanatory comments', 'Names make programs run faster', 'Names replace the need for tests', 'Names are required by all compilers'],
        0,
        'Descriptive names help readers understand purpose at a glance, which is critical because code is read more often than written.',
      ),
      q(
        'What does the Single Responsibility Principle suggest for functions?',
        ['Each function should do one thing at one level of abstraction', 'Each function must be exactly ten lines long', 'Each function should access a global variable', 'Each function should return null on success'],
        0,
        'Functions that do one thing are easier to test, name, and reuse without unexpected side effects.',
      ),
      q(
        'What does DRY stand for?',
        ['Don\'t Repeat Yourself', 'Do Review Yearly', 'Define Runtime Yield', 'Deploy Rapidly Yesterday'],
        0,
        'DRY encourages extracting duplicated logic so fixes and improvements happen in one place.',
      ),
      q(
        'When is duplication sometimes acceptable?',
        ['When two similar blocks change for different reasons and abstraction would obscure intent', 'Never, under any circumstances', 'Only in test files', 'Only when using global variables'],
        0,
        'Blind deduplication can create fragile abstractions; similar code with different evolution paths may stay separate.',
      ),
      q(
        'Why are guard clauses considered a clean-code technique?',
        ['They handle invalid cases early and reduce nested indentation', 'They eliminate the need for error types', 'They make functions run in parallel', 'They replace logging'],
        0,
        'Early returns keep the main logic flat and readable instead of buried inside nested if blocks.',
      ),
      q(
        'What is wrong with an empty catch block?',
        ['It swallows errors and hides failures from callers and operators', 'It improves performance', 'It is required for async code', 'It automatically retries the operation'],
        0,
        'Silent catches make debugging extremely difficult because failures leave no trace.',
      ),
      q(
        'Why do teams use automated formatters and linters?',
        ['To enforce consistent style so reviews focus on logic', 'To replace code reviews entirely', 'To convert code to another language', 'To remove comments automatically'],
        0,
        'Consistent formatting reduces noise in diffs and lets reviewers concentrate on behavior and design.',
      ),
      q(
        'What is a sign that a function should be split?',
        ['It mixes multiple levels of abstraction or unrelated responsibilities', 'It uses more than one parameter', 'It has a return statement', 'It is called from more than one place'],
        0,
        'Mixing low-level details with high-level orchestration in one function obscures its purpose.',
      ),
      q(
        'What does "leave the campground cleaner than you found it" mean in software?',
        ['Make small improvements to code you touch, such as renaming or simplifying', 'Delete all legacy code immediately', 'Rewrite the entire module on every bug fix', 'Avoid writing tests'],
        0,
        'Incremental cleanup prevents rot and spreads quality improvements across the codebase over time.',
      ),
      q(
        'Which quality best describes clean code for new team members?',
        ['It is readable and navigable without extensive tribal knowledge', 'It uses the fewest lines possible regardless of clarity', 'It avoids all external libraries', 'It never changes after the first commit'],
        0,
        'Clean code lowers onboarding cost because structure and naming communicate design decisions.',
      ),
    ],
  ),
  lesson(
    'SOLID Principles',
    [
      'SOLID is a set of five object-oriented design principles that help teams build maintainable, flexible software. The acronym stands for Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, and Dependency Inversion. These principles are not rigid rules but guidelines that reduce coupling and increase cohesion so systems can evolve without cascading breakage.',
      'The Single Responsibility Principle (SRP) states that a class or module should have only one reason to change. If a User class both persists data to a database and formats invoices, a change to billing rules forces edits in the same file as database schema changes. Splitting responsibilities into focused types isolates change and clarifies ownership.',
      'The Open/Closed Principle (OCP) encourages types that are open for extension but closed for modification. Instead of editing a switch statement every time a new payment method appears, you introduce a PaymentProcessor interface and register new implementations. Existing code stays stable while behavior grows through new classes or plugins rather than risky edits to proven paths.',
      'The Liskov Substitution Principle (LSP) requires that subtypes be substitutable for their base types without breaking client expectations. If Square extends Rectangle but violates width/height independence, callers expecting rectangle behavior fail. Correct inheritance preserves contracts: preconditions, postconditions, and invariants must remain valid for all substitutable implementations.',
      'The Interface Segregation Principle (ISP) warns against forcing clients to depend on methods they do not use. One fat interface with print, scan, and fax forces a simple printer class to implement scan and fax with NotSupported exceptions. Smaller role-specific interfaces keep dependents lean and prevent accidental coupling to unused capabilities.',
      'The Dependency Inversion Principle (DIP) says high-level modules should not depend on low-level modules; both should depend on abstractions. Application services depend on a Repository interface, not a concrete PostgresRepository class. Inversion enables swapping storage, mocking in tests, and composing behavior from interchangeable parts without rewriting business logic.',
    ],
    [
      q(
        'What does the "S" in SOLID represent?',
        ['Single Responsibility Principle', 'Simple Syntax Principle', 'Static Scope Principle', 'Secure Socket Principle'],
        0,
        'SRP limits a module to one reason to change, improving cohesion and isolating change.',
      ),
      q(
        'According to OCP, how should new behavior typically be added?',
        ['By extension through new types or plugins rather than modifying stable code', 'By editing every switch statement in the codebase', 'By duplicating entire classes', 'By disabling tests temporarily'],
        0,
        'Extension without modification reduces regression risk in already-tested code paths.',
      ),
      q(
        'LSP is primarily concerned with what?',
        ['Subtypes honoring the behavioral contract of their base types', 'Sorting lists in linear time', 'Minimizing memory usage', 'Using the shortest variable names'],
        0,
        'Clients must be able to use subclasses wherever the base type is expected without surprises.',
      ),
      q(
        'Which scenario violates the Interface Segregation Principle?',
        ['A client is forced to implement unused methods from a large interface', 'A client uses a small, focused interface', 'Two interfaces are composed where needed', 'An adapter bridges incompatible APIs'],
        0,
        'Fat interfaces create dummy implementations and hide true dependencies.',
      ),
      q(
        'Dependency Inversion encourages depending on what?',
        ['Abstractions such as interfaces or protocols, not concrete implementations', 'Global singletons only', 'Hard-coded class names in business logic', 'Database drivers directly in UI code'],
        0,
        'Abstractions decouple policy from details and simplify testing with fakes or mocks.',
      ),
      q(
        'A class that both sends emails and calculates tax violates which principle most directly?',
        ['Single Responsibility Principle', 'Liskov Substitution Principle', 'Open/Closed Principle only', 'None of them'],
        0,
        'Two unrelated reasons to change—messaging and tax rules—belong in separate modules.',
      ),
      q(
        'How does OCP relate to the Strategy pattern?',
        ['Strategy adds new algorithms via new classes without changing the context', 'Strategy removes all interfaces', 'Strategy forbids polymorphism', 'Strategy requires global variables'],
        0,
        'Strategy embodies extension by registering alternative behaviors behind a common interface.',
      ),
      q(
        'Why can violating LSP cause subtle bugs?',
        ['Callers rely on base-type guarantees that broken subclasses do not uphold', 'It always causes compile errors', 'It only affects CSS styling', 'It improves performance automatically'],
        0,
        'Broken substitutability means polymorphic code paths behave differently than expected.',
      ),
      q(
        'ISP helps reduce what kind of coupling?',
        ['Clients coupling to capabilities they never invoke', 'Clients coupling to unit tests', 'Clients coupling to version control', 'Clients coupling to CPU cores'],
        0,
        'Segregated interfaces expose only what each client needs.',
      ),
      q(
        'In DIP, what should high-level policy code depend on?',
        ['Abstract repository or service interfaces', 'Concrete file paths and SQL strings scattered in controllers', 'Random number generators', 'UI widget class names'],
        0,
        'Business rules stay stable when infrastructure details sit behind abstractions.',
      ),
    ],
  ),
  lesson(
    'Design Patterns Intro',
    [
      'Design patterns are reusable solutions to recurring problems in software design. They were popularized by the Gang of Four catalog in the 1990s and describe collaborations between classes and objects that have proven effective across languages and domains. Patterns are not copy-paste code snippets but vocabulary and structure that teams use to communicate design intent quickly.',
      'Patterns sit at a level above individual algorithms. An algorithm solves a computational problem step by step; a pattern organizes classes, responsibilities, and object relationships. Knowing patterns helps you recognize when you are reinventing Observer when events would suffice, or when a Facade would simplify a sprawling subsystem API.',
      'Patterns are grouped into creational, structural, and behavioral categories. Creational patterns deal with object creation mechanics. Structural patterns compose classes into larger structures. Behavioral patterns assign responsibilities and communication flows between objects. Understanding the category tells you which kind of problem the pattern addresses.',
      'Patterns trade flexibility against complexity. Applying Factory Method where a simple constructor suffices adds indirection without benefit. The goal is to introduce a pattern when variation, extension, or decoupling requirements justify the extra types and indirection. YAGNI—you aren\'t gonna need it—applies until repeated change proves otherwise.',
      'Modern frameworks embed patterns implicitly. React\'s component model echoes Composite; Redux and event buses echo Observer and Mediator. Recognizing the underlying pattern helps you use the framework idiomatically and debug when state flow feels wrong. Patterns also appear in distributed systems: Circuit Breaker and Saga are enterprise patterns for resilience and long-running transactions.',
      'Learning patterns improves design discussions. Saying "this looks like a Strategy for pricing rules" is faster than describing the entire class diagram in prose. Combined with SOLID principles and clean code habits, patterns form a toolkit for evolving systems without painting yourself into rigid, untestable corners.',
    ],
    [
      q(
        'What are design patterns primarily?',
        ['Reusable solutions to recurring design problems expressed as structure and collaboration', 'Guaranteed bug-free code templates', 'Compiler optimizations', 'Database indexing strategies'],
        0,
        'Patterns document proven object and class relationships, not specific language syntax.',
      ),
      q(
        'Who popularized the classic catalog of 23 design patterns?',
        ['Gang of Four (Gamma, Helm, Johnson, Vilsides)', 'Tim Berners-Lee', 'Grace Hopper alone', 'The IEEE standards body'],
        0,
        'The Gang of Four book established shared vocabulary used in interviews and design reviews.',
      ),
      q(
        'How do patterns differ from algorithms?',
        ['Patterns organize object relationships; algorithms define step-by-step computation', 'They are identical concepts', 'Patterns only apply to hardware', 'Algorithms never use loops'],
        0,
        'Algorithms focus on operations; patterns focus on architecture of collaborating objects.',
      ),
      q(
        'Which three categories classify classic GoF patterns?',
        ['Creational, structural, behavioral', 'Frontend, backend, DevOps', 'Sync, async, parallel', 'Public, private, protected'],
        0,
        'These categories map to creation, composition, and interaction concerns.',
      ),
      q(
        'What risk comes from overusing patterns?',
        ['Unnecessary complexity and indirection without real variation needs', 'Automatic performance gains', 'Elimination of all bugs', 'Forced use of global variables'],
        0,
        'Patterns should earn their place by solving flexibility or decoupling problems.',
      ),
      q(
        'YAGNI in the context of patterns means what?',
        ['Do not add pattern structure until a concrete need appears', 'Always implement every pattern on day one', 'Never refactor', 'Avoid writing documentation'],
        0,
        'Premature pattern application obscures simple designs.',
      ),
      q(
        'Which real-world framework concept often reflects the Observer pattern?',
        ['Event listeners and pub/sub notification systems', 'Static HTML only', 'Manual memory allocation', 'Binary search trees exclusively'],
        0,
        'Observers decouple publishers from subscribers through notification interfaces.',
      ),
      q(
        'Why is pattern vocabulary useful in teams?',
        ['It communicates design intent concisely and consistently', 'It replaces the need for code', 'It prevents all coupling', 'It eliminates code reviews'],
        0,
        'Shared names shorten design discussions and align implementations.',
      ),
      q(
        'Circuit Breaker is an example of what kind of pattern scope?',
        ['Enterprise/integration pattern for fault tolerance in distributed systems', 'A creational GoF pattern only', 'A CSS layout pattern', 'A sorting algorithm'],
        0,
        'Enterprise patterns address reliability and integration beyond single-process OOP.',
      ),
      q(
        'Patterns work best when combined with what?',
        ['SOLID principles and clean code practices', 'Copy-paste from unknown sources without context', 'Avoiding all abstractions', 'Global mutable state'],
        0,
        'Patterns are tools within broader maintainability and design discipline.',
      ),
    ],
  ),
  lesson(
    'Creational Patterns',
    [
      'Creational patterns abstract the instantiation process. They help a system become independent of how objects are created, composed, and represented. When construction logic spreads across the codebase, changes to object graphs become painful. Centralizing creation makes it easier to swap implementations, enforce invariants at birth, and hide complex assembly steps.',
      'Factory Method defines an interface for creating an object but lets subclasses decide which class to instantiate. A DocumentCreator base class exposes createDocument(), while PdfCreator and WordCreator return specific document types. Callers depend on the creator abstraction, not concrete product classes, which supports Open/Closed extension.',
      'Abstract Factory provides an interface for creating families of related objects without specifying concrete classes. A UIFactory might produce matching Button and Checkbox widgets for Windows or macOS themes. Switching themes means swapping one factory instance rather than scattered news calls throughout the UI layer.',
      'Builder separates construction of a complex object from its representation. A QueryBuilder or HTTP request builder sets fields step by step and returns an immutable product. Builders shine when objects have many optional parameters and telescoping constructors would become unreadable or error-prone.',
      'Prototype creates new objects by copying an existing instance, often through clone methods or structured cloning. Prototypes help when object creation is expensive or when objects are configured templates. JavaScript\'s object spread and structuredClone echo prototype-style duplication for configuration objects.',
      'Singleton ensures a class has one instance and global access—often criticized because it hides dependencies and complicates testing. Prefer dependency injection of a shared instance over static getInstance() globals. When true single-instance semantics are required, document thread safety and lifecycle clearly.',
    ],
    [
      q(
        'What problem do creational patterns primarily address?',
        ['Object creation, composition, and hiding concrete instantiation details', 'Sorting large datasets', 'Rendering CSS animations', 'Network packet routing'],
        0,
        'They decouple clients from knowing which concrete class to instantiate.',
      ),
      q(
        'Factory Method differs from Simple Factory by what trait?',
        ['Subclasses override creation, supporting polymorphic extension via inheritance or virtual methods', 'It cannot return interfaces', 'It forbids testing', 'It only works in C++'],
        0,
        'Factory Method uses polymorphic creators; a simple factory is often a single function.',
      ),
      q(
        'When is Abstract Factory most appropriate?',
        ['When you must create coordinated families of related products', 'When you need exactly one global object', 'When objects have no fields', 'When you never swap implementations'],
        0,
        'Abstract Factory guarantees compatible sets such as themed UI controls.',
      ),
      q(
        'Builder pattern helps avoid what code smell?',
        ['Telescoping constructors with many optional parameters', 'Excessive comments', 'Using too many interfaces', 'Immutable objects'],
        0,
        'Stepwise construction with a fluent API keeps complex object assembly readable.',
      ),
      q(
        'Prototype pattern is useful when what condition holds?',
        ['Creating from scratch is costly or configured templates already exist', 'Objects must never be copied', 'Only static methods are allowed', 'Memory is unlimited and free'],
        0,
        'Cloning configured prototypes avoids repeating expensive setup.',
      ),
      q(
        'What is a common criticism of Singleton?',
        ['Hidden global state complicates testing and tight coupling', 'It always improves performance', 'It is banned in all languages', 'It prevents memory leaks automatically'],
        0,
        'Global access points obscure dependencies and encourage mutable shared state.',
      ),
      q(
        'Which creational pattern best supports swapping entire product families at runtime?',
        ['Abstract Factory', 'Prototype only', 'Builder only', 'None—creational patterns never swap'],
        0,
        'Injecting a different factory changes all related products consistently.',
      ),
      q(
        'Factory Method supports which SOLID principle most directly?',
        ['Open/Closed Principle', 'Liskov Substitution only in databases', 'None', 'Interface Segregation for files'],
        0,
        'New product types extend via new creators without modifying existing client code.',
      ),
      q(
        'In JavaScript, object spread for config duplication resembles which pattern?',
        ['Prototype', 'Singleton', 'Flyweight', 'Bridge'],
        0,
        'Copying an existing object as a template mirrors prototype-style creation.',
      ),
      q(
        'Where should complex object assembly logic ideally live?',
        ['In a dedicated factory or builder, not scattered across callers', 'In every caller that needs the object', 'Only in CSS files', 'In environment variables exclusively'],
        0,
        'Centralized creation enforces invariants and simplifies changes to object graphs.',
      ),
    ],
  ),
  lesson(
    'Structural Patterns',
    [
      'Structural patterns explain how to assemble classes and objects into larger structures while keeping those structures flexible and efficient. They focus on composition, wrapping, and simplifying interfaces rather than creation or algorithmic behavior. When subsystems grow, structural patterns help you present a coherent surface to clients without exposing every internal class.',
      'Adapter converts the interface of a class into another interface clients expect. Wrapping a third-party payment SDK behind your own PaymentGateway interface lets the rest of the application stay stable when the vendor API changes. Adapters are essential at system boundaries where you do not control external code.',
      'Decorator attaches additional responsibilities to an object dynamically without subclass explosion. A base Coffee can be wrapped by MilkDecorator and SugarDecorator, each adding cost and description. Decorators preserve the component interface and compose behavior at runtime instead of creating MilkSugarCoffee subclasses for every combination.',
      'Facade provides a simplified interface to a complex subsystem. A CheckoutFacade might orchestrate inventory, payment, and shipping services behind one placeOrder method. Facades reduce coupling from many clients to many subsystem classes and document the typical usage path for common operations.',
      'Composite lets clients treat individual objects and compositions uniformly. A FileSystemNode interface might be implemented by File and Directory; directories contain children that are also FileSystemNodes. Tree structures in UI component libraries and organizational charts often use Composite.',
      'Proxy controls access to another object, adding lazy loading, access control, caching, or remote communication. A virtual proxy delays loading a heavy image until it is needed; a protection proxy checks permissions before delegating. Proxies implement the same interface as the real subject so clients remain unaware of the indirection.',
    ],
    [
      q(
        'Structural patterns focus on what aspect of design?',
        ['How classes and objects are composed into larger structures', 'How CPU caches work', 'How to sort arrays', 'How to write SQL queries only'],
        0,
        'They address wrapping, simplifying, and organizing object relationships.',
      ),
      q(
        'Adapter pattern is most useful when what situation occurs?',
        ['An existing class interface does not match what clients need', 'You want exactly one instance globally', 'You need to sort data faster', 'You must avoid all interfaces'],
        0,
        'Adapters translate incompatible interfaces at integration boundaries.',
      ),
      q(
        'Decorator avoids what problem compared to deep inheritance hierarchies?',
        ['Subclass explosion for every combination of features', 'Need for any interfaces', 'Use of composition', 'Runtime flexibility'],
        0,
        'Wrapping at runtime combines features without a class for every permutation.',
      ),
      q(
        'What does a Facade intentionally hide?',
        ['Subsystem complexity behind a simpler coordinated entry point', 'All business rules from everyone including admins', 'The existence of tests', 'Error messages from logs'],
        0,
        'Facades document and encapsulate common multi-step operations.',
      ),
      q(
        'Composite pattern applies naturally to what data shape?',
        ['Tree structures where leaves and containers share an interface', 'Linear linked lists only', 'Hash tables exclusively', 'Single integers'],
        0,
        'Uniform treatment of part and whole simplifies recursive operations on trees.',
      ),
      q(
        'Which Proxy type delays expensive creation until needed?',
        ['Virtual proxy', 'Protection proxy only', 'Remote proxy only', 'Adapter proxy'],
        0,
        'Virtual proxies stand in for heavy subjects until access triggers loading.',
      ),
      q(
        'How must Proxy relate to its real subject?',
        ['Implement the same interface so clients delegate transparently', 'Use a completely unrelated API', 'Replace all method names', 'Forbid delegation'],
        0,
        'Interface compatibility lets proxies substitute for real subjects.',
      ),
      q(
        'Bridge pattern separates what two dimensions?',
        ['Abstraction from implementation so each can vary independently', 'Frontend from users', 'CPU from RAM', 'Comments from code'],
        0,
        'Bridge decouples interface hierarchy from platform-specific implementations.',
      ),
      q(
        'Flyweight reduces memory by sharing what?',
        ['Intrinsic shared state across many fine-grained objects', 'All mutable instance fields', 'Database connections only', 'Network bandwidth exclusively'],
        0,
        'Extrinsic state stays outside shared flyweight instances.',
      ),
      q(
        'Wrapping a legacy API for internal use is typically which pattern?',
        ['Adapter', 'Observer', 'Command', 'State'],
        0,
        'Adapters make legacy or third-party APIs fit application interfaces.',
      ),
    ],
  ),
  lesson(
    'Behavioral Patterns',
    [
      'Behavioral patterns concern communication, assignment of responsibilities, and algorithms between objects. They help you decouple senders and receivers, vary algorithms independently, and model complex control flow without tangled conditional logic. When objects know too much about each other\'s internals, behavioral patterns introduce mediating abstractions.',
      'Observer defines a one-to-many dependency so when one object changes state, dependents update automatically. Event emitters, model-view bindings, and domain event buses use this pattern. Observers reduce coupling compared to polling or hard-coded calls from a subject to every interested party.',
      'Strategy encapsulates interchangeable algorithms behind a common interface. A SortStrategy or PricingStrategy lets the context delegate behavior at runtime. Strategy eliminates large switch statements that grow every time marketing adds a promotion type or operations adds a shipping rule.',
      'Command encapsulates a request as an object, enabling undo, queuing, logging, and macro operations. Text editors store DeleteCommand objects on a stack for undo; job queues serialize commands for workers. Commands decouple invokers from receivers and parameterize actions.',
      'State lets an object alter behavior when its internal state changes, appearing to change class. A TCP connection behaves differently when Closed versus Established; state objects replace nested switch on enum anti-patterns. Each state class implements transitions valid for that state, localizing rules.',
      'Chain of Responsibility passes a request along a chain of handlers until one handles it. Middleware in web frameworks, logging pipelines, and approval workflows use chains. Handlers do not need to know the full chain structure; adding a handler means linking it without rewriting predecessors.',
    ],
    [
      q(
        'Behavioral patterns primarily address what?',
        ['Object collaboration, responsibility assignment, and communication flows', 'Memory allocation in kernels', 'File system block sizes', 'HTML tag nesting rules'],
        0,
        'They organize how objects interact and delegate behavior.',
      ),
      q(
        'Observer pattern reduces coupling by what mechanism?',
        ['Subjects notify observers through a subscription interface without knowing concrete observer classes', 'Eliminating all events', 'Using only global variables', 'Forbidding callbacks'],
        0,
        'Publish-subscribe decouples state owners from reactive dependents.',
      ),
      q(
        'Strategy pattern replaces what common anti-pattern?',
        ['Large conditional switches selecting algorithms at runtime', 'All use of interfaces', 'Unit testing', 'Immutable data'],
        0,
        'Encapsulated strategies make new algorithms pluggable without editing context code.',
      ),
      q(
        'Command pattern enables which feature in undoable editors?',
        ['Storing executable command objects on a stack for undo/redo', 'Deleting all history automatically', 'Preventing user input', 'Running only one command ever'],
        0,
        'Commands capture invocations with enough state to reverse or replay them.',
      ),
      q(
        'State pattern helps when what is true?',
        ['Object behavior varies significantly with internal state', 'Objects never change fields', 'Only static methods exist', 'State must be stored globally'],
        0,
        'State objects encapsulate behavior per state instead of monolithic switches.',
      ),
      q(
        'Chain of Responsibility fits which architecture style?',
        ['Middleware pipelines where each handler may pass or process a request', 'Single monolithic function with no delegation', 'Only relational databases', 'Static HTML sites'],
        0,
        'Each handler decides to process or forward along the chain.',
      ),
      q(
        'Mediator pattern centralizes what?',
        ['Communication between colleagues to reduce direct many-to-many links', 'All database schemas', 'CPU scheduling', 'CSS color palettes'],
        0,
        'A mediator coordinates interactions so objects do not reference each other directly.',
      ),
      q(
        'Template Method defines what in a base class?',
        ['Skeleton of an algorithm with hooks for subclass steps', 'Only abstract classes with no implementation', 'Global singleton accessors', 'Random number generation'],
        0,
        'The template controls flow; subclasses override specific steps.',
      ),
      q(
        'Iterator pattern provides what benefit?',
        ['Uniform traversal of aggregates without exposing internal representation', 'Faster network IO automatically', 'Mandatory use of arrays only', 'Removal of all loops'],
        0,
        'Clients traverse collections through a standard interface.',
      ),
      q(
        'Visitor pattern separates what from object structure?',
        ['Operations applied to elements of an object structure', 'Object creation entirely', 'All error handling', 'Network protocols'],
        0,
        'Visitors add new operations without modifying every element class.',
      ),
    ],
  ),
  lesson(
    'Testing Strategies',
    [
      'Testing strategies define how teams verify software correctness at different levels of scope and cost. The test pyramid suggests many fast unit tests, fewer integration tests, and even fewer end-to-end tests. Inverted pyramids with heavy UI automation become slow and flaky, delaying feedback. Balance coverage with maintainability and execution speed.',
      'Unit tests isolate a function or class, replacing dependencies with test doubles such as mocks, stubs, or fakes. They run in milliseconds and pinpoint failures precisely. Good unit tests follow Arrange-Act-Assert: set up state, invoke behavior, assert outcomes. Tests should be deterministic—no reliance on wall-clock timing, network, or random order without seeding.',
      'Integration tests verify that modules work together: database repositories against a real or containerized database, HTTP handlers against an in-memory server, message consumers with a test broker. They catch wiring mistakes unit tests miss but cost more to run and debug. Contract tests between services ensure producers and consumers agree on API shapes.',
      'End-to-end tests simulate user journeys through the full stack in a browser or mobile client. They provide high confidence for critical flows like checkout or login but are slow and brittle when selectors or timing change. Reserve E2E for happy paths and regressions that lower levels cannot catch.',
      'Test-driven development writes a failing test first, then minimal code to pass, then refactors. TDD encourages small steps and designs for testability. Behavior-driven development expresses scenarios in readable Given-When-Then form for collaboration with non-developers. Both emphasize executable specifications over documentation that drifts from reality.',
      'Continuous integration runs the test suite on every commit or pull request. Flaky tests—those that pass and fail without code changes—must be fixed or quarantined because they erode trust. Coverage metrics guide gaps but do not guarantee quality; assert meaningful behavior, not implementation details that break on harmless refactors.',
    ],
    [
      q(
        'What does the test pyramid recommend?',
        ['Many unit tests, fewer integration tests, fewest end-to-end tests', 'Only manual testing', 'E2E tests for every function', 'No unit tests at all'],
        0,
        'Fast tests at the base give rapid feedback; expensive tests sit at the top sparingly.',
      ),
      q(
        'What is the purpose of a mock in unit testing?',
        ['Verify interactions with dependencies or replace slow/external collaborators', 'Replace all production code permanently', 'Eliminate assertions', 'Run tests in parallel on GPU only'],
        0,
        'Mocks isolate the unit under test and can assert expected collaboration.',
      ),
      q(
        'Arrange-Act-Assert structure helps tests by what?',
        ['Separating setup, execution, and verification for readability', 'Removing the need for assertions', 'Making tests nondeterministic', 'Avoiding test names'],
        0,
        'Clear phases make failures easier to diagnose.',
      ),
      q(
        'Integration tests are especially good at catching what?',
        ['Wiring errors between real components such as ORM and database', 'Typos in comments', 'CSS color contrast only', 'Git merge conflicts'],
        0,
        'They exercise boundaries that mocks might misrepresent.',
      ),
      q(
        'Why are E2E tests often flaky?',
        ['They depend on UI timing, network, and full-stack variability', 'They never touch the browser', 'They run too quickly', 'They avoid the database'],
        0,
        'Full-stack variability introduces race conditions and environmental sensitivity.',
      ),
      q(
        'What is the red-green-refactor cycle associated with?',
        ['Test-driven development', 'Waterfall documentation', 'Manual QA only', 'Production debugging'],
        0,
        'TDD starts with a failing test, makes it pass, then improves design.',
      ),
      q(
        'Contract testing between microservices verifies what?',
        ['Consumer and producer agree on API request/response formats', 'CPU usage is minimal', 'All code uses the same variable names', 'UI colors match brand guidelines'],
        0,
        'Contracts prevent integration breaks when services evolve independently.',
      ),
      q(
        'Why should tests avoid asserting private implementation details?',
        ['Refactors that preserve behavior should not break tests unnecessarily', 'Private methods never run', 'Public APIs are always unstable', 'It improves flakiness'],
        0,
        'Tests tied to internals create maintenance burden without protecting users.',
      ),
      q(
        'What should happen to flaky tests in CI?',
        ['Fix root cause or quarantine them—they destroy confidence in the suite', 'Ignore forever', 'Delete the entire suite', 'Run only on Fridays'],
        0,
        'Unreliable CI blocks shipping and hides real regressions.',
      ),
      q(
        'High code coverage alone does not guarantee quality because what?',
        ['Tests may not assert meaningful behavior or edge cases', 'Coverage tools are always wrong', 'Unit tests are impossible', 'Integration tests are free'],
        0,
        'Coverage measures lines executed, not correctness of expectations.',
      ),
    ],
  ),
  lesson(
    'Refactoring',
    [
      'Refactoring improves the internal structure of code without changing external behavior observable to users. Martin Fowler\'s catalog names safe transformations: extract method, rename, inline, move field, replace conditional with polymorphism. Refactoring is not rewriting for aesthetics—it is disciplined change backed by tests that prove behavior stays the same.',
      'The first step before refactoring is ensuring a safety net of automated tests. Without tests, you cannot know if extract class broke an edge case. When legacy code lacks tests, characterization tests capture current behavior before structural changes. Small commits and frequent runs keep feedback loops tight.',
      'Code smells signal when refactoring may help. Long methods, large classes, feature envy, shotgun surgery, and duplicated logic indicate design stress. Not every smell requires immediate action—prioritize hotspots that change often or block new features. Refactor when adding features in the same area, not randomly during unrelated tickets.',
      'Extract Method pulls a block into a named function, clarifying intent and enabling reuse. Rename Variable and Rename Function update call sites with IDE support so names match domain language. Replace Magic Number with Named Constant documents units and business meaning. These mechanical refactors reduce bugs during later feature work.',
      'Deeper refactors change type hierarchies: pull up method, push down field, replace inheritance with delegation. Replace Conditional with Polymorphism removes growing switch statements when new variants appear weekly. Introduce Parameter Object tames long parameter lists. Each transformation has prerequisites and trade-offs documented in refactoring guides.',
      'Refactoring in production codebases happens continuously, not in big-bang rewrite projects. Boy Scout Rule—leave code cleaner than you found it—spreads improvement. When tests fail during refactor, revert or fix forward immediately; never disable tests to greenwash CI. Pair refactoring with code review to spread knowledge and catch missed call sites.',
    ],
    [
      q(
        'Refactoring changes what?',
        ['Internal structure without altering externally observable behavior', 'User-visible features every time', 'Only comments, never code', 'Production data schemas always'],
        0,
        'Behavior preservation is the defining constraint of refactoring.',
      ),
      q(
        'What is the essential safety net for refactoring?',
        ['Automated tests that detect unintended behavior changes', 'Manual hope', 'Disabling CI', 'Deleting legacy code without reading it'],
        0,
        'Tests let you refactor confidently and revert quickly when something breaks.',
      ),
      q(
        'Extract Method refactoring helps by what?',
        ['Naming a block of code and reducing method length', 'Inlining all functions into one giant method', 'Removing all parameters', 'Eliminating types'],
        0,
        'Named extracted methods communicate intent and simplify the caller.',
      ),
      q(
        'What is a "code smell"?',
        ['A surface indicator of deeper design problems, not a bug by itself', 'A syntax error', 'A security vulnerability always', 'A passing test'],
        0,
        'Smells suggest refactoring opportunities when they impede change.',
      ),
      q(
        'When is the best time to refactor a module you are modifying?',
        ['While implementing a related feature, keeping changes scoped and tested', 'Never during feature work', 'Only after a full rewrite approval', 'Without running tests for speed'],
        0,
        'Refactoring in context avoids unrelated churn and pays down debt where it matters.',
      ),
      q(
        'Replace Conditional with Polymorphism suits what situation?',
        ['Growing switches or if-else chains over a type or variant enum', 'Single-branch if statements', 'Configuration files', 'Static constants only'],
        0,
        'Polymorphism localizes variant behavior in dedicated types.',
      ),
      q(
        'What are characterization tests used for in legacy code?',
        ['Document current behavior before structural changes when specs are missing', 'Replace all unit tests permanently', 'Skip integration testing', 'Measure CSS performance'],
        0,
        'They lock down existing outputs so refactors do not silently change behavior.',
      ),
      q(
        'Why avoid big-bang rewrites when incremental refactoring is possible?',
        ['Long freezes deliver no value while risk accumulates without feedback', 'Rewrites are always faster', 'Tests are unnecessary in rewrites', 'Users prefer no updates'],
        0,
        'Incremental change ships value and validates direction continuously.',
      ),
      q(
        'Magic numbers should often be replaced with what?',
        ['Named constants that explain domain meaning and units', 'Random strings', 'Global mutable variables', 'Comments only, never constants'],
        0,
        'Named constants make calculations self-documenting and safer to change.',
      ),
      q(
        'If refactoring breaks a test, what should you do first?',
        ['Determine whether the test caught a real regression or needs updating for intentional change', 'Delete the test silently', 'Disable CI', 'Ship anyway'],
        0,
        'Failed tests are signals—either fix the refactor or clarify requirements.',
      ),
    ],
  ),
  lesson(
    'Code Reviews',
    [
      'Code review is a structured practice where peers examine changes before they merge into shared branches. Reviews spread knowledge, catch defects early, enforce standards, and improve design through dialogue. They are not gatekeeping for ego—they are collaborative quality control that makes the whole team stronger.',
      'Effective reviews focus on correctness, clarity, security, performance implications, and test adequacy. Ask whether the change solves the right problem, handles edge cases, and fits architectural direction. Nitpicks about personal style should be minimized when linters handle formatting; spend time on logic and maintainability instead.',
      'Authors should keep pull requests small and scoped. A five-hundred-line diff across twelve modules overwhelms reviewers and hides bugs. One logical change per PR with a clear description, linked issue, and test evidence respects reviewer time and speeds merge cycles.',
      'Reviewers leave constructive comments: explain why something is risky and suggest alternatives. Approve when requirements are met even if you would have written it differently, unless issues are substantive. Blocking reviews should cite concrete problems—missing validation, race condition, broken API contract—not vague preferences.',
      'Security-sensitive changes deserve extra scrutiny: authentication, authorization, input validation, secrets handling, and dependency upgrades. Check for injection risks, improper logging of PII, and missing rate limits. Automated SAST and dependency scanners complement human review but do not replace contextual judgment.',
      'Review culture affects retention and velocity. Fast turnaround—same day when possible—prevents painful rebases and context loss. Praise good patterns publicly. Use review checklists for common concerns. Over time, teams internalize standards and reviews shift from catching basics to refining design.',
    ],
    [
      q(
        'What is a primary goal of code review?',
        ['Improve quality and share knowledge before code merges', 'Assign blame for bugs', 'Replace all automated tests', 'Increase pull request size'],
        0,
        'Reviews catch issues early and align the team on design and standards.',
      ),
      q(
        'Why should pull requests stay small?',
        ['Large diffs are harder to review thoroughly and merge safely', 'Small PRs cannot include tests', 'Git forbids large changes', 'CI runs only on large PRs'],
        0,
        'Focused changes get better attention and faster feedback.',
      ),
      q(
        'Reviewers should prioritize comments about what?',
        ['Correctness, design, security, and missing tests over personal style', 'Variable naming preferences only', 'Author\'s typing speed', 'IDE theme choices'],
        0,
        'Substantive issues protect users; style should be automated where possible.',
      ),
      q(
        'What should a good PR description include?',
        ['Purpose, scope, linked issue, and how it was tested', 'Only the word "fix"', 'Unrelated refactoring with no explanation', 'Secrets for production'],
        0,
        'Context helps reviewers evaluate changes without reverse-engineering intent.',
      ),
      q(
        'When is it appropriate to block a merge request?',
        ['When there is a concrete defect, security issue, or missing critical test', 'When code style differs slightly but passes linter', 'When you would write it differently but it works', 'When the author is new'],
        0,
        'Blocks should protect users and system integrity, not enforce taste.',
      ),
      q(
        'Automated scanners in review workflows help with what?',
        ['Finding known vulnerability patterns and dependency issues at scale', 'Replacing human judgment entirely', 'Writing feature code automatically', 'Eliminating the need for descriptions'],
        0,
        'Tools catch classes of bugs; humans assess business logic and architecture.',
      ),
      q(
        'Constructive review feedback should often include what?',
        ['Explanation of the concern and a suggested direction', 'Sarcasm only', 'Silent disapproval', 'Personal attacks'],
        0,
        'Actionable feedback teaches and unblocks authors quickly.',
      ),
      q(
        'Why is fast review turnaround important?',
        ['Authors retain context and avoid painful long-lived branches', 'It removes the need for tests', 'It prevents all bugs', 'It eliminates documentation'],
        0,
        'Delayed reviews increase merge conflicts and slow delivery.',
      ),
      q(
        'Security reviews should scrutinize changes involving what?',
        ['Auth, input validation, secrets, and data exposure', 'Comment wording only', 'Font choices', 'Whitespace in markdown only'],
        0,
        'Security mistakes in these areas have high impact.',
      ),
      q(
        'Healthy review culture includes what behavior?',
        ['Recognizing good patterns and mentoring through comments', 'Only negative feedback', 'Avoiding all discussion', 'Merging without reading diffs'],
        0,
        'Positive reinforcement and teaching improve long-term code quality.',
      ),
    ],
  ),
  lesson(
    'API Design',
    [
      'API design shapes how clients integrate with your services and how easily those integrations evolve. Whether REST, GraphQL, gRPC, or event streams, good APIs are predictable, consistent, and documented. Breaking changes without versioning frustrate consumers and multiply support burden. Design for the caller\'s mental model, not internal database tables.',
      'RESTful HTTP APIs use resources identified by URLs, standard verbs, and meaningful status codes. GET retrieves, POST creates, PUT/PATCH updates, DELETE removes. Nouns in paths—/users/42/orders—beat verb-heavy RPC-style paths like /getUserOrders. Use 201 for creation with Location header, 404 for missing resources, 409 for conflicts.',
      'Versioning strategies include URL prefixes (/v2/), headers (Accept-Version), or compatibility layers. Prefer additive changes: new optional fields, new endpoints. When breaking change is unavoidable, ship a new version and provide migration windows and deprecation notices. Never silently change semantics of existing fields.',
      'Pagination, filtering, and sorting should be consistent across list endpoints. Cursor-based pagination scales better than large offsets for deep pages. Document default page sizes and maximum limits. Return total counts only when cheap to compute or approximate clearly.',
      'Error responses need machine-readable codes and human-readable messages. A JSON body with error code, message, and optional field-level details helps clients display UI and retry correctly. Do not leak stack traces or internal IDs to public consumers. Idempotency keys for POST operations prevent duplicate charges on retries.',
      'Documentation—OpenAPI/Swagger, GraphQL schema introspection, or proto files—should stay synchronized with implementation. Examples and authentication instructions reduce integration time. Design reviews for public APIs include naming, auth scopes, rate limits, and backward compatibility before coding spreads inconsistent patterns.',
    ],
    [
      q(
        'Good API design prioritizes what perspective?',
        ['The consumer\'s mental model and integration experience', 'Internal database table names exposed verbatim', 'Maximum number of endpoints regardless of need', 'Undocumented behavior changes'],
        0,
        'APIs are products for developers; clarity and stability matter.',
      ),
      q(
        'Which HTTP method is idiomatic for creating a new resource?',
        ['POST', 'GET', 'DELETE', 'TRACE'],
        0,
        'POST creates resources; 201 Created with Location is common response practice.',
      ),
      q(
        'Why prefer noun-based resource paths in REST?',
        ['They model resources consistently and map cleanly to HTTP verbs', 'Verbs in URLs are always faster', 'Nouns prevent authentication', 'REST forbids verbs entirely in all contexts'],
        0,
        'Resources plus HTTP methods express operations without redundant action names in paths.',
      ),
      q(
        'What is the safest default for evolving public APIs?',
        ['Additive compatible changes with clear deprecation for breaking changes', 'Silent field renames', 'Removing endpoints without notice', 'Changing types of existing fields'],
        0,
        'Backward compatibility preserves existing client integrations.',
      ),
      q(
        'Cursor-based pagination helps mainly with what problem?',
        ['Stable, efficient paging through large datasets compared to deep offsets', 'Eliminating all databases', 'Encrypting responses', 'Removing need for limits'],
        0,
        'Offsets become slow and inconsistent under concurrent writes; cursors track position.',
      ),
      q(
        'What should error responses include for clients?',
        ['Structured code, message, and optional field details without internal stack traces', 'Full server stack traces publicly', 'Only HTTP status with empty body always', 'Random binary data'],
        0,
        'Structured errors enable programmatic handling and safe user messaging.',
      ),
      q(
        'Idempotency keys on POST are used to what?',
        ['Prevent duplicate side effects when clients retry failed requests', 'Speed up GET requests', 'Replace authentication', 'Disable logging'],
        0,
        'Retries are common on networks; idempotency avoids double processing.',
      ),
      q(
        'OpenAPI specifications primarily document what?',
        ['HTTP API endpoints, schemas, parameters, and responses', 'CPU microcode', 'CSS themes', 'Git history'],
        0,
        'OpenAPI gives machine-readable contracts and generates docs and clients.',
      ),
      q(
        'Which status code fits a resource conflict such as duplicate unique email?',
        ['409 Conflict', '200 OK', '301 Moved Permanently', '418 I\'m a teapot as standard choice'],
        0,
        '409 signals the request conflicts with current server state.',
      ),
      q(
        'gRPC compared to JSON REST often offers what advantage?',
        ['Efficient binary serialization and strong contracts via protobuf', 'Human-readable browser debugging without tools', 'No need for schema', 'Automatic UI generation always'],
        0,
        'Protobuf contracts and HTTP/2 suit internal high-performance service communication.',
      ),
    ],
  ),
  lesson(
    'Concurrency Basics',
    [
      'Concurrency means structuring a program so multiple tasks make progress overlapping in time. Parallelism executes tasks simultaneously on multiple cores. Concurrent design appears in web servers handling thousands of connections, background job workers, UI threads, and distributed systems. Correct concurrency requires clear models of shared state and synchronization.',
      'Threads and processes are common execution units. Processes have isolated memory; threads share memory within a process. Shared mutable state without coordination causes data races—undefined behavior in many languages. Mutexes, locks, and synchronized blocks serialize access to critical sections but risk deadlock if lock ordering is inconsistent.',
      'Immutability reduces race conditions because readers need no locks when data does not change. Functional patterns copy-on-write or pass messages instead of sharing mutable objects. JavaScript\'s single-threaded event loop avoids traditional thread races in user code but still faces async ordering bugs and shared worker state.',
      'Async/await and promises model concurrent I/O without blocking threads. A Node server awaits database calls while handling other requests on the same thread. Still, CPU-bound work on the event loop blocks all clients—offload heavy computation to worker threads or process pools. Distinguish I/O concurrency from CPU parallelism.',
      'Deadlock occurs when threads wait on each other circularly; livelock when they keep reacting without progress. Avoid holding locks while calling unknown code. Use timeouts, lock ordering conventions, and higher-level primitives like channels or actor mailboxes. Structured concurrency scopes tie task lifetimes to parent tasks to prevent orphaned work.',
      'Testing concurrent code requires stress tests, thread sanitizers, and deterministic schedulers where available. Prefer declarative concurrency—parallel map with bounded pool—over manual thread management when libraries support it. Document invariants and which threads may touch which data. Simplicity beats heroic locking schemes.',
    ],
    [
      q(
        'What is the difference between concurrency and parallelism?',
        ['Concurrency structures overlapping tasks; parallelism executes them simultaneously on multiple cores', 'They are identical terms', 'Concurrency requires exactly one core', 'Parallelism forbids shared memory'],
        0,
        'You can be concurrent on one core via scheduling; parallelism needs multiple executors.',
      ),
      q(
        'What causes a data race?',
        ['Unsynchronized concurrent access to shared mutable state', 'Using immutable objects only', 'Reading constants', 'Single-threaded execution'],
        0,
        'Races produce unpredictable results when writes interleave without coordination.',
      ),
      q(
        'How does immutability help concurrent programs?',
        ['Readers can share data safely without locks when nothing mutates', 'It eliminates the need for CPUs', 'It prevents all async code', 'It requires global variables'],
        0,
        'Immutable data removes a major class of synchronization bugs.',
      ),
      q(
        'Why should CPU-heavy work avoid blocking the JavaScript event loop?',
        ['It stalls all concurrent requests handled on that thread', 'It improves UI responsiveness automatically on all loops', 'Node forbids workers', 'It only affects CSS'],
        0,
        'Long synchronous CPU work prevents processing other I/O callbacks.',
      ),
      q(
        'What is deadlock?',
        ['Threads permanently waiting on each other\'s held locks', 'Fast termination of all threads', 'Successful lock acquisition', 'A type of unit test'],
        0,
        'Circular wait conditions freeze affected threads unless broken by timeouts or design fixes.',
      ),
      q(
        'Async/await primarily helps with what kind of concurrency?',
        ['Non-blocking I/O while waiting on network, disk, or timers', 'Automatic multi-core CPU parallelism always', 'Removing all callbacks forever', 'Eliminating need for error handling'],
        0,
        'Await yields control during I/O waits without blocking OS threads excessively.',
      ),
      q(
        'A mutex or lock is used to what?',
        ['Ensure only one thread accesses a critical section at a time', 'Speed up all code paths', 'Replace databases', 'Parse JSON faster'],
        0,
        'Mutual exclusion prevents interleaved corruption of shared structures.',
      ),
      q(
        'Actor model concurrency communicates how?',
        ['Message passing between isolated actors without shared mutable state', 'Global variables only', 'Busy-wait loops exclusively', 'Direct pointer sharing without copies'],
        0,
        'Actors encapsulate state and process mailboxes sequentially.',
      ),
      q(
        'What tool helps detect race conditions during testing?',
        ['Thread sanitizer or stress tests with many interleavings', 'CSS linter', 'Spell checker', 'Only manual code review never'],
        0,
        'Dynamic analysis tools flag suspicious concurrent accesses.',
      ),
      q(
        'Structured concurrency aims to prevent what problem?',
        ['Orphaned background tasks outliving their parent scope', 'All use of async', 'Memory allocation', 'Using thread pools'],
        0,
        'Parent scopes cancel or await children, tying lifetimes together.',
      ),
    ],
  ),
  lesson(
    'Performance Optimization',
    [
      'Performance optimization makes systems meet latency, throughput, and resource budgets. The first rule is measure before optimizing—profilers, traces, and benchmarks reveal real bottlenecks instead of guessed ones. Premature optimization wastes effort on code that rarely runs while missing hot paths that dominate user experience.',
      'Algorithmic complexity matters at scale. Replacing O(n²) nested loops with hash map lookups often beats micro-optimizing constant factors. Choose appropriate data structures: heaps for priority queues, bloom filters for probabilistic membership, B-trees for disk-friendly indexing. Big-O analysis guides when implementation details become relevant.',
      'Caching trades memory for speed at many layers: CPU caches, application memoization, HTTP CDN edge caches, database buffer pools, and distributed Redis. Cache invalidation is hard—define TTLs, version keys, and event-driven eviction. Stale cache bugs are subtle; document consistency guarantees explicitly.',
      'Database performance hinges on indexes matching query patterns, avoiding N+1 ORM queries, and limiting selected columns. Explain plans show sequential scans versus index use. Connection pooling reuses expensive connections. Read replicas offload reporting queries from primary writers.',
      'Front-end performance targets critical rendering path: minimize blocking JavaScript, compress images, lazy load below-fold content, and reduce layout thrashing. Core Web Vitals—LCP, INP, CLS—measure real user experience. Bundle splitting and tree shaking shrink JavaScript payloads.',
      'Optimization ends with verification: load tests simulate expected traffic plus headroom; regressions get benchmarks in CI for critical paths. Document SLAs and degradation strategies—circuit breakers, graceful fallbacks, queue backpressure. Faster code that wrong answers is worthless; correctness remains the baseline.',
    ],
    [
      q(
        'What should precede most performance optimization work?',
        ['Measurement and profiling to find actual bottlenecks', 'Rewriting everything in assembly', 'Disabling tests', 'Adding caches everywhere blindly'],
        0,
        'Data-driven optimization targets code that dominates latency or CPU.',
      ),
      q(
        'Why can improving algorithmic complexity outweigh micro-optimizations?',
        ['Better asymptotic behavior scales when input size grows', 'Micro-optimizations always worsen Big-O', 'Complexity never matters in production', 'Compilers ignore loops'],
        0,
        'O(n log n) versus O(n²) dominates as n increases regardless of small constant tweaks.',
      ),
      q(
        'What is the N+1 query problem?',
        ['One query for a list plus one query per item instead of a batched join', 'Running exactly one query total', 'Using too many indexes', 'Caching too aggressively'],
        0,
        'ORM lazy loading often triggers N+1 patterns that hammer databases.',
      ),
      q(
        'Cache invalidation challenges arise because what?',
        ['Stale entries must be detected or expired without serving wrong data indefinitely', 'Caches are always perfectly consistent automatically', 'Memory is infinite', 'Networks are synchronous'],
        0,
        'Without invalidation strategy, caches return outdated results.',
      ),
      q(
        'Database indexes help queries by what mechanism?',
        ['Allowing faster lookups instead of full table scans when selective', 'Deleting unused rows automatically', 'Replacing normalization', 'Preventing all writes'],
        0,
        'Indexes trade write overhead and storage for read speed on matched columns.',
      ),
      q(
        'Core Web Vitals measure what aspect of applications?',
        ['Real user experience metrics like loading and interactivity', 'Server CPU temperature only', 'Git commit frequency', 'Number of classes in code'],
        0,
        'LCP, INP, and CLS reflect perceived front-end performance.',
      ),
      q(
        'Connection pooling improves performance by what?',
        ['Reusing established database connections instead of opening new ones per request', 'Eliminating SQL', 'Running queries sequentially only', 'Removing authentication'],
        0,
        'Handshake and auth costs amortize across pooled connections.',
      ),
      q(
        'Load testing primarily validates what?',
        ['System behavior and SLAs under expected and peak traffic', 'Syntax highlighting', 'Comment quality', 'Variable naming'],
        0,
        'Load tests reveal bottlenecks, errors, and capacity limits before production incidents.',
      ),
      q(
        'Lazy loading assets benefits users by what?',
        ['Deferring non-critical downloads until needed, improving initial load', 'Loading everything upfront always', 'Disabling caching', 'Increasing bundle size'],
        0,
        'Deferred loading reduces work on critical first paint.',
      ),
      q(
        'When optimizing, what must never be sacrificed?',
        ['Correctness of results and observable behavior unless explicitly acceptable', 'All logging', 'All documentation', 'All tests permanently'],
        0,
        'Performance gains that corrupt data or break contracts fail the primary goal.',
      ),
    ],
  ),
  lesson(
    'Security Practices',
    [
      'Security practices embed protection throughout the software lifecycle, not as a final audit checkbox. Threat modeling identifies assets, adversaries, and attack surfaces early. Assume breach: minimize blast radius with least privilege, network segmentation, and secrets rotation. Security is a property of the whole system—code, configuration, deployment, and operations.',
      'Input validation and output encoding prevent injection attacks. Parameterized queries stop SQL injection; contextual escaping stops XSS in HTML, JavaScript, and URL attributes. Never trust client-side validation alone. Validate length, type, and allowed character sets on the server for every external input.',
      'Authentication verifies identity; authorization verifies permission for each action. Store passwords with slow adaptive hashes like bcrypt or Argon2, never plaintext or reversible encryption. Multi-factor authentication raises bar for credential theft. Session tokens should be random, HttpOnly, Secure cookies with sensible expiration and rotation.',
      'Secrets—API keys, database passwords, private keys—belong in secret managers or environment injection, not source control. Scan repositories for accidental commits. Principle of least privilege applies to service accounts and cloud IAM roles. Regular dependency updates patch known CVEs; software composition analysis flags vulnerable libraries.',
      'Transport security uses TLS everywhere data crosses untrusted networks. Certificate pinning may apply for high-risk mobile clients. Security headers—Content-Security-Policy, HSTS, X-Frame-Options—reduce browser-side attack vectors. Log authentication events and anomalies without logging secrets or full payment data.',
      'Secure SDLC includes security-focused code review, static analysis, penetration testing for critical apps, and incident response playbooks. Educate developers on OWASP Top 10 categories: broken access control, cryptographic failures, injection, insecure design, and misconfiguration. Culture beats purely technical controls when people report suspicious activity quickly.',
    ],
    [
      q(
        'Parameterized SQL queries primarily prevent what attack?',
        ['SQL injection', 'Cross-site scripting in CSS only', 'DNS hijacking', 'Buffer overflow in GPU drivers always'],
        0,
        'Parameters separate query structure from untrusted data values.',
      ),
      q(
        'How should passwords be stored?',
        ['Hashed with adaptive algorithms like bcrypt or Argon2 plus unique salt', 'Plaintext for fast login', 'Reversible AES without salt', 'Base64 encoding only'],
        0,
        'Slow hashes resist offline cracking if the hash database leaks.',
      ),
      q(
        'What does least privilege mean for service accounts?',
        ['Grant only permissions required for the task, no broader', 'Grant admin to simplify debugging always', 'Share one root key everywhere', 'Disable all logging'],
        0,
        'Limited permissions contain damage when credentials are compromised.',
      ),
      q(
        'Why is client-side validation insufficient for security?',
        ['Attackers can bypass browsers and send arbitrary requests directly', 'Browsers validate perfectly always', 'Servers cannot read HTTP bodies', 'Validation is illegal on servers'],
        0,
        'Server-side validation is authoritative; client checks are UX only.',
      ),
      q(
        'Content-Security-Policy headers help mitigate what?',
        ['Cross-site scripting by restricting script and resource sources', 'SQL injection in databases directly', 'Hard disk failure', 'Git merge conflicts'],
        0,
        'CSP limits which origins can execute scripts and load assets.',
      ),
      q(
        'Where should production secrets live?',
        ['Secret managers or secure environment injection, not committed code', 'Public GitHub repositories', 'Hard-coded in frontend JavaScript', 'Screenshot attachments'],
        0,
        'Committed secrets are scanned by attackers and leak in history forever.',
      ),
      q(
        'Authentication differs from authorization in what way?',
        ['Authentication proves identity; authorization decides allowed actions', 'They are the same step', 'Authorization happens before identity exists', 'Authentication only applies to CSS'],
        0,
        'Knowing who you are does not imply you may perform every operation.',
      ),
      q(
        'Multi-factor authentication improves security by what?',
        ['Requiring additional proof beyond stolen passwords', 'Eliminating passwords entirely always', 'Removing need for TLS', 'Logging users out faster only'],
        0,
        'Second factors block abuse of leaked credentials.',
      ),
      q(
        'Dependency scanning tools address what risk?',
        ['Known vulnerabilities in third-party libraries', 'Slow CSS rendering only', 'Incorrect variable names', 'Missing comments'],
        0,
        'SCA alerts teams to CVEs in transitive dependencies.',
      ),
      q(
        'Threat modeling early in design helps teams what?',
        ['Identify assets and attacks before code hardens wrong assumptions', 'Skip all testing', 'Remove encryption for speed', 'Publish internal APIs publicly by default'],
        0,
        'Proactive modeling guides controls where they matter most.',
      ),
    ],
  ),
  lesson(
    'Architecture Styles',
    [
      'Architecture styles define high-level structure: how components divide responsibilities, communicate, and deploy. Choosing a style trades simplicity, scalability, team autonomy, and operational cost. No single architecture fits every product—context includes team size, domain complexity, latency requirements, and release cadence.',
      'Monolithic architecture bundles UI, business logic, and data access into one deployable unit. It simplifies development, debugging, and transactions when the team and codebase are small. Growth can strain build times and scaling granularity, but many successful products stay modular monoliths with clear internal boundaries until true distribution is justified.',
      'Layered (n-tier) architecture separates presentation, application, domain, and infrastructure layers. Dependencies point inward toward domain logic. Violations—SQL in controllers, UI logic in entities—create tight coupling. Layering clarifies testing boundaries when enforced with interfaces.',
      'Microservices split the system into independently deployable services around business capabilities. Teams own services end to end, choosing tech stacks where appropriate. Benefits include isolated scaling and failure domains; costs include distributed tracing, network latency, data consistency challenges, and DevOps maturity requirements. Avoid microservices prematurely.',
      'Event-driven architecture propagates state changes through events on a message broker. Producers emit facts; consumers react asynchronously. It decouples services temporally and enables audit logs and fan-out workflows. Challenges include ordering guarantees, idempotent consumers, schema evolution, and debugging cross-service flows.',
      'Other styles include serverless (functions as a unit of scale), CQRS (separate read/write models), hexagonal/ports-and-adapters (domain isolated from IO), and space-based (processing grids for extreme scale). Document architecture decisions with ADRs capturing context, decision, and consequences so future teams understand why the shape exists.',
    ],
    [
      q(
        'What does choosing an architecture style primarily trade off?',
        ['Simplicity, scalability, team autonomy, and operational complexity', 'Only programming language syntax', 'IDE theme colors', 'Number of comments required'],
        0,
        'Each style optimizes for different organizational and technical constraints.',
      ),
      q(
        'Monolithic architecture is often best when what is true?',
        ['Team and domain complexity are modest and rapid iteration matters', 'Hundreds of teams need independent deploys day one', 'Every service must use different databases by policy', 'Network partitions are desired'],
        0,
        'Monoliths reduce distributed systems overhead early in product life.',
      ),
      q(
        'Microservices increase which operational concern?',
        ['Distributed tracing, deployment coordination, and inter-service consistency', 'Need for any networking', 'Use of databases', 'Writing functions'],
        0,
        'Distribution shifts complexity from code structure to operations and observability.',
      ),
      q(
        'Event-driven architecture decouples services primarily in what dimension?',
        ['Time—asynchronous reaction to events rather than synchronous calls only', 'Space—by forbidding all messages', 'Version control', 'CPU cache lines'],
        0,
        'Events let producers emit without waiting for all consumers to finish.',
      ),
      q(
        'Hexagonal architecture emphasizes what?',
        ['Isolating domain logic from external IO via ports and adapters', 'Maximizing SQL in UI templates', 'Single global variable state', 'Removing all interfaces'],
        0,
        'Ports define application boundaries; adapters plug in databases, UI, and APIs.',
      ),
      q(
        'CQRS separates what?',
        ['Command (write) models from query (read) models', 'Frontend from electricity', 'Tests from code always', 'Logs from metrics exclusively in all systems'],
        0,
        'Different read and write shapes optimize respective workloads when justified.',
      ),
      q(
        'Layered architecture dependency rule typically says what?',
        ['Inner domain layers should not depend on outer infrastructure details', 'Database calls belong in UI templates', 'All layers may import anything', 'Only one layer may exist'],
        0,
        'Dependency inversion keeps business rules independent of frameworks.',
      ),
      q(
        'Architecture Decision Records capture what?',
        ['Context, decision, and consequences of significant structural choices', 'Daily standup notes only', 'Every variable rename', 'Employee vacation schedules'],
        0,
        'ADRs preserve rationale when original authors leave.',
      ),
      q(
        'Premature microservices often cause what problem?',
        ['Operational overhead without commensurate team or scaling benefits', 'Automatic bug elimination', 'Zero network latency', 'Free monitoring'],
        0,
        'Distribution costs should be paid when boundaries and scale demand them.',
      ),
      q(
        'Serverless architecture scales primarily by what unit?',
        ['Individual functions responding to events', 'Entire monolithic VMs only', 'Manual hardware racks', 'Static HTML files exclusively'],
        0,
        'Function-level billing and scaling suit spiky, event-triggered workloads.',
      ),
    ],
  ),
  lesson(
    'Documentation',
    [
      'Documentation communicates intent, usage, and constraints to future readers—including yourself six months later. Good docs reduce onboarding time, support incidents, and prevent repeated questions. Documentation lives at multiple levels: inline comments for non-obvious logic, README files for setup, API references for integrators, and architecture overviews for system thinkers.',
      'README files should explain what the project does, prerequisites, install steps, configuration, how to run tests, and how to contribute. Copy-pasteable commands beat vague prose. Keep the README current when scripts change; stale setup instructions erode trust faster than no README. Link to deeper docs rather than duplicating everything.',
      'API documentation generated from schemas stays aligned with behavior when CI verifies drift. Include authentication, rate limits, error codes, and versioning policy. Tutorials walk through common tasks end to end; reference docs list every endpoint exhaustively. Both audiences matter—new integrators and experienced maintainers.',
      'Architecture documentation diagrams major components, data flows, and external dependencies. C4 models zoom from system context to containers, components, and optional code. Document failure modes, SLAs, and data retention. Runbooks tell operators how to restart services, rotate keys, and respond to alerts at 3 a.m.',
      'Comments explain why, not what obvious code already shows. Replace outdated comments during refactors—lying comments are worse than none. Docstrings on public functions describe parameters, return values, exceptions, and side effects. Examples in docs beat abstract descriptions for complex APIs.',
      'Documentation is part of Definition of Done for features that change public behavior. Review docs in pull requests like code. Prefer living documents near the repository over wiki pages that diverge silently. International audiences benefit from clear, plain language and glossaries for domain terms.',
    ],
    [
      q(
        'What is a primary purpose of project documentation?',
        ['Transfer knowledge and reduce repeated questions for future maintainers', 'Replace all tests', 'Increase code obfuscation', 'Eliminate code reviews'],
        0,
        'Docs capture intent and procedures that code alone does not express.',
      ),
      q(
        'A good README should include what?',
        ['Purpose, setup steps, configuration, and how to run tests', 'Only the license file', 'Private production passwords', 'Unrelated marketing copy only'],
        0,
        'README is the first stop for developers cloning the repository.',
      ),
      q(
        'Comments should primarily explain what?',
        ['Non-obvious rationale and constraints, not restate obvious syntax', 'Every line of code regardless of clarity', 'Historical typos only', 'Secrets for convenience'],
        0,
        'Why-comments survive refactors better than narrating what the code literally does.',
      ),
      q(
        'Why generate API docs from OpenAPI or schema sources?',
        ['Keeps reference material synchronized with actual endpoints and types', 'Prevents all breaking changes automatically', 'Removes need for examples', 'Hides authentication requirements'],
        0,
        'Generated docs reduce drift between implementation and published reference.',
      ),
      q(
        'Runbooks are intended for whom?',
        ['Operators handling incidents and routine maintenance tasks', 'Only new interns on day one', 'Competitors', 'End users shopping online'],
        0,
        'Runbooks guide restarts, failover, key rotation, and alert response.',
      ),
      q(
        'C4 architecture modeling provides what benefit?',
        ['Layered views from system context down to components for different audiences', 'Automatic code generation only', 'Replacement for all logs', 'CSS styling guidelines'],
        0,
        'C4 helps stakeholders zoom to appropriate detail levels.',
      ),
      q(
        'What makes outdated documentation harmful?',
        ['It misleads readers into wrong setup steps or unsafe operations', 'It always auto-updates', 'It improves security', 'It speeds onboarding incorrectly in a good way'],
        0,
        'Wrong docs waste time and can cause outages or security mistakes.',
      ),
      q(
        'Docstrings on public APIs should describe what?',
        ['Parameters, returns, exceptions, and important side effects', 'Author\'s favorite music', 'Git blame information only', 'Random UUIDs'],
        0,
        'Complete docstrings enable IDE tooltips and accurate client usage.',
      ),
      q(
        'When should documentation be updated?',
        ['When behavior, setup, or public interfaces change—as part of the same change', 'Never after initial release', 'Only by a separate documentation team years later', 'Only when marketing requests'],
        0,
        'Docs in the same PR as code changes stay accurate.',
      ),
      q(
        'Tutorials differ from reference documentation by what?',
        ['Tutorials teach task-oriented flows; reference exhaustively lists capabilities', 'Tutorials list every parameter alphabetically', 'Reference is always outdated', 'There is no difference'],
        0,
        'Learning paths and exhaustive specs serve different reader needs.',
      ),
    ],
  ),
]

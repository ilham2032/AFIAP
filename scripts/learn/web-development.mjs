import { lesson, q } from './helpers.mjs'

export const webDevelopmentSections = [
  lesson(
    'Introduction to the Web',
    [
      'The World Wide Web is a global system of interconnected documents and applications accessed through the internet. When you type a URL into your browser or click a link, your browser acts as a client that sends an HTTP request to a remote web server. The server locates the requested resource—HTML pages, CSS stylesheets, JavaScript files, images, or JSON data—and sends a response back over the network. The browser then parses these files and renders them into the interactive page you see on screen.',
      'HTML (HyperText Markup Language) defines the structure and semantic meaning of web content. It tells the browser what is a heading, a paragraph, a navigation menu, or an image. CSS (Cascading Style Sheets) controls visual presentation including colors, typography, spacing, borders, and layout. JavaScript adds behavior and interactivity, allowing pages to respond to user clicks, validate form input, fetch new data from servers, and update content dynamically without a full page reload.',
      'Every web request begins with a URL (Uniform Resource Locator), such as https://example.com/products?id=42. The browser first uses DNS (Domain Name System) to translate the human-readable domain name into a numeric IP address that identifies the server. Communication between client and server typically happens over HTTP or its encrypted counterpart HTTPS, which uses TLS to protect data in transit from eavesdropping and tampering.',
      'Websites can be static, meaning the server delivers pre-built files exactly as stored on disk, or dynamic, meaning server-side code generates HTML or JSON on each request using databases and application logic. Modern single-page applications often load a minimal HTML shell once and use JavaScript to fetch and render data from REST or GraphQL APIs, creating fast, app-like experiences in the browser.',
      'Browser DevTools are an essential part of every web developer\'s workflow. The Elements panel lets you inspect and live-edit HTML and CSS, the Network panel shows every request and response with timing data, and the Console displays JavaScript errors and log output. Learning to debug effectively in DevTools saves hours of guesswork and is expected in professional development environments.',
      'Accessibility, performance, and security are foundational concerns rather than afterthoughts. Fast-loading pages retain users and rank higher in search results, accessible pages work for people using screen readers or keyboard-only navigation, and secure practices protect user data from common threats. Building these qualities in from the start produces better products and reduces costly rework later.',
    ],
    [
      q('What role does the browser play in the client-server model of the web?', ['It acts as a client that requests resources and renders the response', 'It acts as a database server storing user accounts', 'It registers domain names through DNS', 'It compiles CSS into binary machine code'], 0, 'The browser is the client—it sends HTTP requests to web servers and assembles the returned HTML, CSS, and JavaScript into a rendered page.'),
      q('Which technology is primarily responsible for defining the structure and meaning of web content?', ['CSS', 'HTML', 'JavaScript', 'SQL'], 1, 'HTML provides semantic markup that describes headings, paragraphs, links, images, and other content elements.'),
      q('What does DNS do when you visit a website?', ['Translates a domain name like example.com into a server IP address', 'Encrypts all data sent between browser and server', 'Renders HTML into visible pixels on screen', 'Validates JavaScript syntax before execution'], 0, 'DNS resolves human-readable domain names into numeric IP addresses so the browser knows which server to contact.'),
      q('How does HTTPS differ from plain HTTP?', ['HTTPS encrypts data in transit using TLS, protecting it from interception', 'HTTPS removes the need for HTML on web pages', 'HTTPS is only used for downloading images, not HTML', 'HTTPS and HTTP are identical protocols with different names'], 0, 'HTTPS wraps HTTP communication in TLS encryption, which is critical for login forms, payments, and any sensitive data.'),
      q('Which language runs in the browser to add interactivity such as form validation and dynamic updates?', ['HTML', 'JavaScript', 'Markdown', 'YAML'], 1, 'JavaScript executes in the browser and handles user events, DOM manipulation, and asynchronous API calls.'),
      q('What characterizes a static website?', ['The server delivers pre-built files without generating content per request', 'The site contains no CSS or JavaScript at all', 'The site can only be viewed on mobile devices', 'The site stores all data in browser localStorage only'], 0, 'Static sites serve fixed files from disk; dynamic sites generate content on each request using server-side code and databases.'),
      q('Why are browser DevTools important for web developers?', ['They allow inspection of HTML, CSS, network requests, and JavaScript errors during development', 'They automatically deploy websites to production servers', 'They replace the need to write HTML by generating pages from screenshots', 'They compile TypeScript to JavaScript without any build step'], 0, 'DevTools provide real-time insight into page structure, styling, network activity, and runtime errors.'),
      q('What does CSS control on a web page?', ['Visual presentation including colors, fonts, spacing, and layout', 'Database queries and server-side routing', 'DNS record configuration for domain names', 'The encryption keys used by HTTPS'], 0, 'CSS handles all visual styling and layout concerns, from typography to flexbox and grid positioning.'),
      q('What is a URL?', ['A Uniform Resource Locator that identifies the address of a resource on the web', 'A JavaScript framework for building single-page applications', 'An HTML element used to create hyperlinks between pages', 'A CSS property that sets the width of an element'], 0, 'URLs specify the protocol, domain, path, and optional query parameters needed to locate a web resource.'),
      q('Why should developers consider accessibility from the beginning of a project?', ['Accessible sites work for users with disabilities and assistive technologies, reaching a broader audience', 'Accessibility eliminates the need for CSS styling entirely', 'Accessibility prevents JavaScript from running in the browser', 'Accessibility automatically makes pages load faster on all networks'], 0, 'Building accessibility in early supports screen readers, keyboard navigation, and legal compliance while improving usability for everyone.'),
    ],
  ),
  lesson(
    'HTML Document Structure',
    [
      'Every HTML document follows a standard skeleton defined by the HTML5 specification. The <!DOCTYPE html> declaration at the very top tells the browser to render the page in standards mode using modern HTML5 rules rather than legacy quirks mode. Without this declaration, older browsers may fall back to inconsistent rendering behavior that breaks layouts and styling.',
      'The <html> element wraps the entire document and should include a lang attribute, such as lang="en" or lang="fr", to declare the primary language of the page. This attribute helps screen readers pronounce text correctly and allows search engines to serve the page to users searching in that language. There is exactly one <html> element per document.',
      'The <head> section contains metadata that is not displayed directly on the page. Essential elements include <meta charset="UTF-8"> to specify character encoding, <title> to set the browser tab title, and <meta name="viewport" content="width=device-width, initial-scale=1"> for responsive mobile rendering. The head also links external CSS files and defines SEO meta descriptions.',
      'The <body> element contains all visible content: headings, paragraphs, images, navigation, forms, and scripts. A well-formed document has exactly one <head> and one <body> nested inside <html>. Content placed outside these elements is invalid and may be relocated unpredictably by the browser\'s error-recovery algorithm.',
      'Heading hierarchy creates a logical document outline. Use one <h1> per page for the main topic, then <h2> for major sections, <h3> for subsections, and so on through <h6>. Avoid skipping heading levels unnecessarily, as assistive technologies use this hierarchy to help users navigate long pages quickly.',
      'External resources are linked in the head to keep structure, presentation, and behavior separate. Use <link rel="stylesheet" href="styles.css"> for CSS and <script src="app.js" defer></script> for JavaScript that should run after HTML parsing. This separation of concerns makes code easier to maintain, test, and reuse across multiple pages.',
    ],
    [
      q('What is the purpose of the <!DOCTYPE html> declaration?', ['It tells the browser to use HTML5 standards mode for rendering', 'It creates a hyperlink to an external stylesheet', 'It inserts a visible page title in the browser tab', 'It defines the character encoding as UTF-8'], 0, 'The doctype declaration activates standards mode, ensuring consistent HTML5 rendering across browsers.'),
      q('Where does all visible page content belong in an HTML document?', ['Inside the <body> element', 'Inside the <head> element', 'Inside the <!DOCTYPE> declaration', 'Directly inside <html> before <head>'], 0, 'All user-visible elements—text, images, navigation, forms—go inside the body element.'),
      q('How many <h1> elements should a page typically contain?', ['One, representing the main topic of the page', 'As many as needed for every section heading', 'Zero, because h1 is deprecated in HTML5', 'Exactly three for proper SEO balance'], 0, 'A single h1 clarifies the page\'s primary topic for both users and search engines.'),
      q('What does the lang attribute on the <html> element help with?', ['Screen reader pronunciation and search engine language targeting', 'Automatic translation of all page text into English', 'Faster loading of external CSS stylesheets', 'Encryption of data sent over HTTPS'], 0, 'The lang attribute tells assistive technologies which language rules to apply and helps search engines index the page correctly.'),
      q('Which element sets the text displayed in the browser tab?', ['<title>', '<h1>', '<header>', '<meta name="title">'], 0, 'The <title> element inside <head> sets the browser tab label and the default bookmark name.'),
      q('Why is <meta charset="UTF-8"> important in the document head?', ['It ensures special characters and non-Latin scripts display correctly', 'It sets the page background color to white', 'It enables JavaScript execution in older browsers', 'It automatically compresses HTML files for faster transfer'], 0, 'UTF-8 encoding supports virtually all characters worldwide, preventing garbled text from encoding mismatches.'),
      q('What does the viewport meta tag accomplish?', ['It configures how the page scales and displays on mobile device screens', 'It sets the maximum width of the body element in pixels', 'It disables JavaScript on small screens for performance', 'It links the page to a Content Delivery Network'], 0, 'The viewport meta tag tells mobile browsers to use the device width as the layout viewport, enabling responsive design.'),
      q('Where should external CSS files be linked in an HTML document?', ['In the <head> using <link rel="stylesheet">', 'At the bottom of the <body> using <script>', 'Inside an HTML comment at the top of the file', 'In a separate JSON configuration file only'], 0, 'Linking CSS in the head allows styles to apply before content renders, reducing flash of unstyled content.'),
      q('What is the correct nesting order for the main structural elements?', ['<!DOCTYPE html>, then <html> containing <head> and <body>', '<body> containing <html> and <head>', '<head> and <body> as siblings without an <html> wrapper', '<html> inside <body> with <head> at the root'], 0, 'The standard structure is doctype, html wrapper, then head and body as direct children of html.'),
      q('Why should heading levels not be skipped unnecessarily (e.g., h1 directly to h4)?', ['Assistive technologies rely on sequential heading levels to build a navigable document outline', 'Skipped levels cause browsers to reject the HTML as invalid', 'Skipped levels prevent CSS from applying to those headings', 'Skipped levels automatically demote the page in search rankings'], 0, 'Screen readers use heading hierarchy as a table of contents; skipping levels creates a confusing navigation experience.'),
    ],
  ),
  lesson(
    'HTML Semantic Elements',
    [
      'Semantic HTML uses elements whose names describe their meaning and purpose, not just their visual appearance. Tags like <header>, <nav>, <main>, <article>, <section>, <aside>, and <footer> tell browsers, search engines, and assistive technologies what role each part of the page plays. This is fundamentally different from using generic <div> elements with class names, which carry no inherent meaning to parsers.',
      'The <header> element typically wraps introductory content such as a site logo, page title, and primary navigation. A page can have multiple headers—one for the site and one for each <article>. The <nav> element specifically marks navigation links, helping screen readers offer a "skip to navigation" shortcut and allowing search engines to identify site structure.',
      'The <main> element encloses the primary, unique content of the page and should appear only once per document. Content inside <main> should not include repeated elements like global navigation or footers. The <article> element represents a self-contained piece of content that could be syndicated independently, such as a blog post, news story, or forum comment.',
      'The <section> element groups thematically related content and usually includes a heading. Use it when the content forms a distinct section of the document with its own topic. The <aside> element marks tangentially related content like sidebars, pull quotes, or advertising blocks that supplement the main content without being essential to it.',
      'The <footer> element contains closing information for its nearest sectioning ancestor or the whole page, such as copyright notices, contact links, and related resources. Other important semantic elements include <figure> and <figcaption> for images with captions, <time datetime="2026-08-29"> for machine-readable dates, and <mark> for highlighted text.',
      'Choosing semantic elements improves accessibility because assistive technologies map them to landmark roles automatically. It also benefits SEO because search engines weight content inside <main> and <article> more heavily than generic containers. When no semantic element fits, <div> remains appropriate, but reaching for semantics first is a hallmark of professional HTML authoring.',
    ],
    [
      q('What is the primary advantage of semantic HTML elements over generic <div> tags?', ['They convey meaning about content purpose to browsers, search engines, and assistive technologies', 'They render faster because browsers skip parsing their attributes', 'They automatically apply CSS styling without any stylesheet', 'They prevent JavaScript from modifying their contents'], 0, 'Semantic tags describe content role, enabling better accessibility, SEO, and code readability.'),
      q('Which element should enclose the primary, unique content of a page exactly once?', ['<main>', '<header>', '<section>', '<div class="main">'], 0, 'The <main> element identifies the central content area and should appear only once per page.'),
      q('When should you use the <article> element?', ['For self-contained content like a blog post that could stand alone or be syndicated', 'For any div that needs a border and padding', 'Only for pages that contain more than one <h1>', 'For wrapping the site-wide navigation menu'], 0, '<article> marks independent, distributable content units such as blog posts, news articles, or user comments.'),
      q('What is the purpose of the <nav> element?', ['To mark a block of navigation links for browsers and assistive technologies', 'To embed a Google Maps navigation widget', 'To create a numbered list of page sections', 'To disable link following for SEO purposes'], 0, '<nav> identifies major navigation blocks, enabling screen reader landmarks and SEO site-structure analysis.'),
      q('Which element is best suited for tangentially related content like a sidebar or pull quote?', ['<aside>', '<footer>', '<main>', '<header>'], 0, '<aside> represents content that is related but not essential to the main flow, such as sidebars and callout boxes.'),
      q('How does the <section> element differ from a generic <div>?', ['<section> groups thematically related content and typically includes its own heading', '<section> can only contain text, not images or forms', '<section> automatically numbers headings h2 through h6', '<section> replaces the need for any CSS layout rules'], 0, 'Sections represent distinct thematic groups within a document and should usually have an associated heading.'),
      q('What landmark role does the <footer> element typically represent?', ['Closing information for a section or the entire page, such as copyright and contact links', 'The primary navigation area at the top of the page', 'The main content area unique to the current page', 'A sidebar advertisement container'], 0, '<footer> holds closing metadata and links for its parent sectioning element or the whole document.'),
      q('Why is <time datetime="2026-08-29"> preferred over plain text for dates?', ['The datetime attribute provides a machine-readable format for parsers and calendar apps', 'The <time> element automatically converts dates to the user\'s local timezone visually', 'The <time> element prevents dates from being indexed by search engines', 'The <time> element renders dates in bold without any CSS'], 0, 'The datetime attribute lets machines parse dates reliably while the visible text can use any human-friendly format.'),
      q('What pair of elements should be used for an image with a descriptive caption?', ['<figure> containing <img> and <figcaption>', '<div> containing <img> and <span>', '<section> containing <img> and <footer>', '<article> containing <img> and <title>'], 0, '<figure> groups media with its caption in <figcaption>, creating a semantic association.'),
      q('How do semantic elements benefit screen reader users specifically?', ['They map to ARIA landmark roles, enabling quick navigation between page regions', 'They disable all JavaScript event listeners within their boundaries', 'They force all text to be read at double speed', 'They hide decorative images from the accessibility tree automatically'], 0, 'Landmark elements like <nav>, <main>, and <aside> let screen reader users jump directly to key page areas.'),
    ],
  ),
  lesson(
    'HTML Text and Links',
    [
      'HTML provides a rich set of elements for marking up text content with appropriate meaning. The <p> element wraps paragraphs of text, while <strong> indicates strong importance and <em> indicates emphasis—both render with default styling but carry semantic weight for assistive technologies. Use <br> sparingly for line breaks within a paragraph rather than creating separate empty paragraphs for spacing.',
      'Headings from <h1> through <h6> establish a document outline and should contain concise, descriptive text. The <blockquote> element marks extended quotations, optionally with a cite attribute pointing to the source URL. Inline quotations use <q>, which browsers typically render with quotation marks automatically. The <cite> element identifies the title of a creative work being referenced.',
      'Hyperlinks are the foundation of the web, created with the <a> element and its href attribute. An absolute URL like href="https://example.com/about" points to an external resource, while a relative URL like href="/contact" or href="team.html" references a resource on the same site. The href value can also be a fragment identifier like href="#section-id" to jump to an element with that id on the same page.',
      'Link attributes control behavior and security. The target="_blank" attribute opens the link in a new tab, but should always be paired with rel="noopener noreferrer" to prevent the new page from accessing window.opener and to omit the referrer header. The download attribute on an anchor suggests the browser save the linked resource rather than navigate to it.',
      'Text-level semantics include <abbr title="HyperText Markup Language"> for abbreviations, <code> for short code snippets, <pre> for preformatted blocks of code, and <kbd> for keyboard input. The <span> element is a generic inline container used when no other semantic inline element applies, often for styling a portion of text within a paragraph.',
      'Accessible link text is descriptive and meaningful out of context. Avoid generic phrases like "click here" or "read more" because screen reader users often navigate by listing all links on a page. Instead, write link text that describes the destination, such as "View our pricing plans" or "Download the 2026 annual report." This practice improves usability for everyone, not just assistive technology users.',
    ],
    [
      q('What is the semantic difference between <strong> and <b> in modern HTML?', ['<strong> conveys strong importance while <b> is purely presentational with no semantic meaning', '<strong> renders bold text and <b> renders italic text', '<strong> can only be used inside headings while <b> works anywhere', '<strong> is deprecated in HTML5 and <b> is the recommended replacement'], 0, 'In HTML5, <strong> carries semantic weight indicating importance, whereas <b> is stylistic without added meaning.'),
      q('How do you create a hyperlink to an external website?', ['<a href="https://example.com">Visit Example</a>', '<link href="https://example.com">Visit Example</link>', '<url>https://example.com</url>', '<a src="https://example.com">Visit Example</a>'], 0, 'The <a> element with an href attribute creates a clickable hyperlink to the specified URL.'),
      q('What does href="#pricing" do when used in a link on the same page?', ['Scrolls the page to the element with id="pricing"', 'Opens a new browser tab showing the pricing page', 'Downloads a file named pricing from the server', 'Sends an HTTP POST request to a /pricing endpoint'], 0, 'Fragment identifiers (hash links) navigate to elements with matching id attributes on the current page.'),
      q('Why should target="_blank" links include rel="noopener noreferrer"?', ['To prevent the opened page from accessing window.opener and to omit the referrer header for security', 'To force the link to open in the same tab instead of a new one', 'To make the link visible only to search engine crawlers', 'To disable JavaScript execution on the newly opened page'], 0, 'Without noopener, the new page can manipulate the original tab via window.opener, creating a security vulnerability.'),
      q('Which element should wrap a multi-sentence quotation from an external source?', ['<blockquote>', '<q>', '<cite>', '<em>'], 0, '<blockquote> is designed for block-level extended quotations, while <q> is for short inline quotes.'),
      q('What is the purpose of the title attribute on an <abbr> element?', ['To provide the full expanded form of the abbreviation on hover and for assistive technologies', 'To set the font size of the abbreviated text', 'To create a hyperlink from the abbreviation to a glossary page', 'To prevent the abbreviation from being translated by browsers'], 0, 'The title attribute on <abbr> reveals the full term, helping users who may not recognize the shorthand.'),
      q('Which element is appropriate for displaying a short inline code snippet within a paragraph?', ['<code>', '<pre>', '<script>', '<kbd>'], 0, '<code> marks inline code fragments, while <pre> is for preformatted blocks preserving whitespace.'),
      q('Why is "click here" considered poor link text for accessibility?', ['Screen reader users navigating by link list cannot determine the destination from generic text', 'The phrase "click here" is blocked by all modern browsers as a security risk', 'Search engines penalize pages that use the words "click" or "here" in links', 'The phrase causes the link to open in a new tab regardless of attributes'], 0, 'Descriptive link text like "View pricing plans" communicates purpose when read out of surrounding context.'),
      q('What does the download attribute on an <a> element suggest to the browser?', ['Save the linked file to the user\'s device instead of navigating to it in the browser', 'Download the entire website as a ZIP archive', 'Require the user to log in before accessing the link', 'Compress the linked file before displaying it inline'], 0, 'The download attribute hints that the resource should be downloaded, useful for PDFs, images, and other files.'),
      q('When should you use a relative URL like href="about.html" instead of an absolute URL?', ['When linking to a page on the same website, keeping links portable across environments', 'When linking to an external website on a different domain', 'When creating an email link to a mail server', 'When linking to a specific section on a different domain'], 0, 'Relative URLs work within the same site and remain valid when the site moves between domains or staging environments.'),
    ],
  ),
  lesson(
    'HTML Lists and Tables',
    [
      'HTML offers three list types for organizing related items. Unordered lists use <ul> with <li> children to present items where sequence does not matter, such as a list of features or ingredients. Ordered lists use <ol> with <li> children when the sequence is meaningful, like step-by-step instructions or ranked items. List items can contain any flow content including nested lists, paragraphs, and images.',
      'Description lists use <dl> (description list) with <dt> (term) and <dd> (description) pairs, ideal for glossaries, metadata displays, and key-value information. Unlike ul and ol, description lists do not use <li> elements. Each <dt> can have multiple <dd> elements, and multiple <dt> elements can share a single <dd>, providing flexible association patterns.',
      'HTML tables represent tabular data using <table>, <thead>, <tbody>, <tfoot>, <tr>, <th>, and <td> elements. The <thead> contains header rows with <th> cells that describe columns or rows, while <tbody> holds the main data rows with <td> cells. Use <tfoot> for summary rows such as totals. Tables should be used for data, not for page layout—a practice largely replaced by CSS flexbox and grid.',
      'The scope attribute on <th> elements (scope="col" or scope="row") explicitly associates header cells with the data cells they describe, improving accessibility for screen readers. The colspan and rowspan attributes allow a single cell to span multiple columns or rows, useful for grouped headers and merged data cells in complex tables.',
      'The <caption> element provides a visible title for the table and should be the first child of <table>. Captions help all users understand the table\'s purpose at a glance. For very large tables, consider whether the data might be better presented as a list or whether pagination and filtering should be handled with JavaScript.',
      'Accessible tables require clear header associations so screen reader users can understand which header belongs to each data cell. Avoid empty header cells, provide text alternatives for any data conveyed only through color, and ensure tab order follows a logical reading sequence. When a table is purely decorative, role="presentation" can remove it from the accessibility tree.',
    ],
    [
      q('Which HTML elements create an unordered list?', ['<ul> containing <li> elements', '<ol> containing <li> elements', '<dl> containing <dt> and <dd> elements', '<list> containing <item> elements'], 0, 'Unordered lists use <ul> as the container with <li> for each item, typically rendered with bullet markers.'),
      q('When should you use an ordered list (<ol>) instead of an unordered list (<ul>)?', ['When the sequence or ranking of items is meaningful, such as step-by-step instructions', 'When items contain more than one paragraph of text', 'When the list will be styled with custom CSS bullets', 'When the list contains fewer than three items'], 0, 'Ordered lists imply sequence; use them for ranked items, procedures, or any list where order matters.'),
      q('What elements make up a description list?', ['<dl> with <dt> for terms and <dd> for descriptions', '<ul> with <dt> for terms and <dd> for descriptions', '<ol> with <term> and <desc> elements', '<table> with <th> and <td> elements'], 0, 'Description lists pair terms (<dt>) with their definitions or values (<dd>) inside a <dl> container.'),
      q('Which table element contains header cells that label columns or rows?', ['<thead> with <th> cells', '<tbody> with <td> cells', '<tfoot> with <caption> cells', '<table> with <header> cells'], 0, 'The <thead> section holds header rows using <th> elements that describe the data below or beside them.'),
      q('What does the scope="col" attribute on a <th> element do?', ['Associates the header cell with all data cells in its column for assistive technologies', 'Sets the column width to match the header text length', 'Hides the column from screen readers while keeping it visible', 'Sorts the column alphabetically when the table loads'], 0, 'The scope attribute explicitly links header cells to their corresponding data cells, aiding screen reader navigation.'),
      q('What is the purpose of the <caption> element in a table?', ['It provides a visible title describing the table\'s content or purpose', 'It hides the table from search engine crawlers', 'It applies a CSS border around the entire table', 'It converts the table data into a CSV download link'], 0, '<caption> gives the table a descriptive title, helping all users understand what data it contains.'),
      q('When should colspan be used on a <td> or <th> element?', ['When a single cell needs to span across multiple columns in the table', 'When a cell contains more text than other cells in the row', 'When a cell should be hidden from the accessibility tree', 'When a cell needs to link to an external URL'], 0, 'colspan merges a cell horizontally across the specified number of columns, common in grouped header rows.'),
      q('Why should tables not be used for page layout in modern web development?', ['CSS flexbox and grid provide accessible, responsive layout without the semantic confusion of data tables', 'Browsers no longer render table elements visually', 'Table elements cannot contain images or hyperlinks', 'Search engines block pages that contain any <table> elements'], 0, 'Layout tables confuse assistive technologies that expect tabular data; CSS layout methods are the correct approach.'),
      q('Can list items (<li>) contain nested lists?', ['Yes, an <li> can contain a nested <ul> or <ol> for hierarchical content', 'No, lists cannot be nested inside other lists in HTML5', 'Only ordered lists can be nested inside unordered lists', 'Nested lists require a special <nest> wrapper element'], 0, 'List items can contain block-level content including other lists, enabling multi-level outlines and menus.'),
      q('What does role="presentation" on a table indicate to assistive technologies?', ['The table is used for visual layout only and should be ignored by the accessibility tree', 'The table contains sensitive data that should not be read aloud', 'The table will automatically update its data from an API', 'The table requires user interaction before its content is accessible'], 0, 'role="presentation" removes layout-only tables from the accessibility tree, preventing confusing data table announcements.'),
    ],
  ),
  lesson(
    'HTML Forms',
    [
      'HTML forms collect user input and send it to a server for processing. The <form> element wraps all form controls and defines how data is submitted using the action attribute (the destination URL) and method attribute (GET or POST). GET appends form data to the URL as query parameters, suitable for searches; POST sends data in the request body, appropriate for sensitive or large submissions.',
      'Common input types include text, email, password, number, date, checkbox, radio, file, and hidden. Each <input> should have a associated <label> connected via the for attribute matching the input\'s id, or by wrapping the input inside the label. Labels are essential for accessibility because they expand the clickable area and provide context to screen readers.',
      'The <textarea> element creates a multi-line text input for longer responses like comments or messages. The <select> element with <option> children creates a dropdown menu, while <select multiple> allows choosing several options. The <button> element creates a clickable button; use type="submit" for form submission, type="reset" to clear fields, and type="button" for JavaScript-triggered actions.',
      'HTML5 validation attributes reduce the need for JavaScript checks on basic input. The required attribute prevents submission of empty fields, minlength and maxlength constrain text length, min and max set numeric or date bounds, and pattern accepts a regular expression for custom format validation. The browser displays native error messages when validation fails on submit.',
      'Fieldset and legend elements group related form controls visually and semantically. Wrap radio button groups or related sections in <fieldset> with a descriptive <legend> title. This grouping helps screen reader users understand which fields belong together and improves the visual organization of complex forms.',
      'Accessible forms provide clear labels, helpful placeholder text (which is not a substitute for labels), descriptive error messages linked with aria-describedby, and logical tab order. Never disable the browser\'s default focus outline without providing an equally visible custom alternative. Test forms with keyboard-only navigation to ensure every control is reachable and operable.',
    ],
    [
      q('What do the action and method attributes on a <form> element control?', ['action sets the submission URL and method sets whether data is sent via GET or POST', 'action sets the CSS styling and method sets the JavaScript validation rules', 'action encrypts the form data and method sets the character encoding', 'action creates a hyperlink and method determines if it opens in a new tab'], 0, 'The action attribute specifies where to send form data; method determines the HTTP verb used for submission.'),
      q('Why should every form input have an associated <label>?', ['Labels provide accessible names for inputs and expand the clickable target area', 'Labels automatically validate the input value against a regex pattern', 'Labels encrypt password fields before they are sent to the server', 'Labels prevent the input from accepting more than 100 characters'], 0, 'Labels connect human-readable text to form controls, which is critical for screen readers and usability.'),
      q('How do you correctly associate a <label> with an <input> that has id="email"?', ['<label for="email">Email</label> paired with <input id="email">', '<label name="email">Email</label> paired with <input name="email">', '<label href="#email">Email</label> paired with <input id="email">', '<label type="email">Email</label> wrapping the input without an id'], 0, 'The for attribute on the label must match the id attribute on the input to create the association.'),
      q('When should you use method="POST" instead of method="GET" for form submission?', ['When submitting sensitive data like passwords or large payloads that should not appear in the URL', 'When the form contains only a single search input field', 'When you want the form data visible in the browser address bar', 'When the form action points to an external website'], 0, 'POST sends data in the request body, keeping it out of the URL—essential for passwords and large datasets.'),
      q('What does the required attribute on an input field do?', ['Prevents form submission until the field has a value, showing a native browser error', 'Automatically fills the field with a default placeholder value', 'Hides the field from users until they click a "Show more" button', 'Encrypts the field value before it is sent to the server'], 0, 'The required attribute triggers built-in HTML5 validation that blocks submission of empty fields.'),
      q('Which element creates a multi-line text input for longer user responses?', ['<textarea>', '<input type="text">', '<select>', '<output>'], 0, '<textarea> provides a resizable multi-line text area, unlike the single-line <input type="text">.'),
      q('What is the purpose of wrapping related radio buttons in a <fieldset> with a <legend>?', ['It groups related controls semantically and visually, helping users and screen readers understand the relationship', 'It ensures only one radio button can ever be selected on the entire page', 'It automatically submits the form when a radio button is selected', 'It converts radio buttons into checkboxes for multi-select behavior'], 0, 'Fieldset and legend create an accessible grouping that screen readers announce when entering the group.'),
      q('What input type should be used for a field that accepts an email address?', ['<input type="email">', '<input type="text" pattern="email">', '<input type="url">', '<textarea type="email">'], 0, 'type="email" provides built-in format validation and displays an appropriate keyboard on mobile devices.'),
      q('What is the difference between type="submit" and type="button" on a <button> element?', ['type="submit" sends the form data while type="button" performs no default action and requires JavaScript', 'type="submit" clears all form fields while type="button" sends the form data', 'type="submit" opens a new browser tab while type="button" closes the current tab', 'type="submit" is deprecated and type="button" is the HTML5 replacement'], 0, 'Submit buttons trigger form submission; plain buttons have no default behavior and are meant for JavaScript handlers.'),
      q('Why is placeholder text not a substitute for a proper <label>?', ['Placeholders disappear when users start typing and are often not announced by screen readers', 'Placeholders prevent the browser from validating the input field', 'Placeholders encrypt the input value for security purposes', 'Placeholders are not supported in any modern browser'], 0, 'Labels remain visible and accessible at all times; placeholders vanish on focus and may not be read by assistive technologies.'),
    ],
  ),
  lesson(
    'CSS Fundamentals',
    [
      'CSS (Cascading Style Sheets) controls the visual presentation of HTML elements. A CSS rule consists of a selector that targets elements and a declaration block containing property-value pairs. For example, p { color: navy; font-size: 16px; } selects all paragraph elements and sets their text color and size. Multiple declarations within a block are separated by semicolons.',
      'Selectors come in many forms. Element selectors like h1 target all instances of a tag. Class selectors like .highlight target elements with a matching class attribute. ID selectors like #main-header target a unique element with that id. More specific selectors include descendant (.nav a), child (.nav > li), attribute ([type="email"]), and pseudo-class (:hover, :focus) selectors.',
      'The cascade determines which rule wins when multiple rules target the same element. Specificity is calculated from selector types: inline styles beat IDs, IDs beat classes, and classes beat elements. When specificity is equal, the rule appearing last in the stylesheet wins. The !important flag overrides normal cascade order but should be used sparingly because it makes debugging difficult.',
      'CSS properties cover typography (font-family, font-size, line-height, text-align), colors (color, background-color, opacity), spacing (margin, padding), borders (border, border-radius), and display behavior. Units include px (pixels), em and rem (relative to font size), % (percentage of parent), vw and vh (viewport dimensions), and fr (fractional units in grid).',
      'The box model concept treats every element as a rectangular box with content, padding, border, and margin layers. By default, width and height apply to the content area only, with padding and border added on top. The property box-sizing: border-box changes this so width and height include padding and border, simplifying layout calculations.',
      'External stylesheets are linked in the HTML head and cached by the browser for faster subsequent page loads. Internal styles go in a <style> tag, and inline styles use the style attribute on individual elements. For maintainability, prefer external stylesheets with class-based selectors over inline styles, which are difficult to override and reuse across pages.',
    ],
    [
      q('What are the two main parts of a CSS rule?', ['A selector that targets elements and a declaration block with property-value pairs', 'An HTML tag and a JavaScript function', 'A class name and an id attribute', 'A media query and a font declaration'], 0, 'Every CSS rule has a selector (who to style) and declarations (what styles to apply).'),
      q('Which selector targets all elements with class="btn"?', ['.btn', '#btn', 'btn', '<btn>'], 0, 'Class selectors are prefixed with a dot: .classname matches elements whose class attribute contains that value.'),
      q('In the CSS cascade, which selector type has the highest specificity?', ['Inline styles applied via the style attribute', 'Element selectors like p and h1', 'Universal selector (*)', 'Pseudo-elements like ::before'], 0, 'Inline styles have the highest specificity, followed by IDs, classes/attributes, and element selectors.'),
      q('What does box-sizing: border-box change about element sizing?', ['Width and height include padding and border, not just the content area', 'All elements become invisible until hovered over', 'Elements are removed from the document flow entirely', 'Padding and border are disabled on all elements'], 0, 'border-box makes the specified width/height encompass content, padding, and border, simplifying layout math.'),
      q('Which CSS property sets the space between an element\'s content edge and its border?', ['padding', 'margin', 'gap', 'spacing'], 0, 'Padding creates space inside the element between the content and the border; margin creates space outside the border.'),
      q('What is the difference between em and rem units?', ['em is relative to the parent element\'s font size while rem is relative to the root html element\'s font size', 'em is absolute pixels and rem is percentage-based', 'em works only for width and rem works only for height', 'em and rem are identical and interchangeable in all contexts'], 0, 'em compounds with nested elements (relative to parent), while rem always references the root font size for consistency.'),
      q('What does the :hover pseudo-class do?', ['Applies styles when the user\'s pointer is over the element', 'Applies styles when the element is the first child of its parent', 'Applies styles when the element contains a valid form input', 'Applies styles only when the page is printed'], 0, ':hover triggers styles during pointer interaction, commonly used for buttons, links, and interactive cards.'),
      q('Why should !important be avoided in most CSS code?', ['It breaks the normal cascade and makes styles extremely difficult to override and debug', 'It prevents CSS from being cached by the browser', 'It disables all pseudo-class selectors on the element', 'It converts external stylesheets into inline styles automatically'], 0, '!important overrides specificity calculations, leading to specificity wars and maintenance headaches.'),
      q('Which approach is recommended for applying styles across an entire multi-page website?', ['Link an external stylesheet with <link rel="stylesheet"> in each page\'s head', 'Use inline style attributes on every element in every page', 'Embed all CSS inside JavaScript template literals', 'Apply styles only through browser user-agent defaults'], 0, 'External stylesheets are cached, reusable, and keep presentation separate from HTML structure.'),
      q('What does the descendant selector .nav a match?', ['Any <a> element inside an element with class="nav", at any nesting depth', 'Only direct child <a> elements of .nav', 'Every <a> element on the entire page regardless of location', 'Only <a> elements that themselves have class="nav"'], 0, 'A space between selectors creates a descendant combinator, matching elements nested at any level inside the ancestor.'),
    ],
  ),
  lesson(
    'CSS Box Model',
    [
      'The CSS box model describes how elements occupy space on a page. Every element is rendered as a rectangular box composed of four layers: content, padding, border, and margin. The content area holds the text, images, or other media. Padding surrounds the content inside the border. The border wraps the padding, and margin creates transparent space outside the border separating the element from its neighbors.',
      'With the default box-sizing: content-box, the width and height properties set only the content area dimensions. Padding and border widths are added on top, so an element with width: 200px, padding: 20px, and border: 5px actually occupies 250px horizontally. This behavior surprises many beginners and causes layout overflow issues.',
      'Setting box-sizing: border-box on an element (or globally with * { box-sizing: border-box; }) makes width and height include content, padding, and border. A border-box element with width: 200px, padding: 20px, and border: 5px still occupies exactly 200px total. Most modern CSS resets apply border-box globally for this reason.',
      'Margin collapses vertically between adjacent block-level elements: the larger of two adjoining vertical margins wins rather than them being added together. For example, two paragraphs each with margin-bottom: 20px and margin-top: 20px will have only 20px of space between them, not 40px. Horizontal margins never collapse.',
      'Borders are styled with border-width, border-style (solid, dashed, dotted, none), and border-color. Shorthand border: 2px solid black sets all three at once. The border-radius property rounds corners, accepting one to four values for individual corner control. Outline is similar to border but does not affect layout space—it draws outside the border for focus indicators.',
      'Understanding the box model is essential for precise layout work. Use browser DevTools to inspect the box model diagram for any element, showing exact content, padding, border, and margin values. When elements appear misaligned or overflow their containers, the box model is usually the first place to investigate.',
    ],
    [
      q('What are the four layers of the CSS box model, from inside to outside?', ['Content, padding, border, margin', 'Margin, border, padding, content', 'Content, border, margin, padding', 'Padding, content, margin, border'], 0, 'From innermost to outermost: content holds the media, padding adds inner space, border outlines the element, margin adds outer space.'),
      q('With the default content-box sizing, what does width: 300px mean?', ['The content area is 300px wide, with padding and border added on top of that', 'The total element including padding and border is exactly 300px wide', 'The margin area is 300px wide on each side', 'The element is 300px wide only when the viewport exceeds 768px'], 0, 'In content-box mode, width applies to the content area alone; padding and border increase the total rendered size.'),
      q('How does box-sizing: border-box affect width calculations?', ['The specified width includes content, padding, and border combined', 'It removes padding and border from the element entirely', 'It doubles the width to account for left and right padding', 'It applies only to inline elements, not block elements'], 0, 'border-box makes the width property represent the total visible width, making layouts more predictable.'),
      q('What happens when two block elements have adjacent vertical margins of 30px and 20px?', ['The margins collapse and only 30px of space appears between them', 'Both margins apply and 50px of space appears between them', 'The browser removes both margins entirely', 'The smaller margin is doubled to match the larger one'], 0, 'Vertical margin collapse means the larger margin wins; the smaller one is ignored in the collapsed result.'),
      q('Which property creates space outside an element\'s border, separating it from neighboring elements?', ['margin', 'padding', 'gap', 'border-spacing'], 0, 'Margin is the outermost layer of the box model, creating transparent space between elements.'),
      q('What does border: 1px solid #ccc set?', ['A 1-pixel solid border with color #ccc on all four sides', 'A 1-pixel dashed outline for focus states only', 'A 1-pixel padding with color #ccc inside the content', 'A 1-pixel margin with color #ccc outside the border'], 0, 'The border shorthand sets width, style, and color in one declaration, applying to all sides.'),
      q('How is outline different from border in CSS?', ['Outline does not affect layout space and is commonly used for focus indicators', 'Outline replaces the border on all elements by default', 'Outline can only be applied to inline elements', 'Outline automatically animates on hover unlike border'], 0, 'Outlines draw outside the border without changing element dimensions, ideal for accessible focus rings.'),
      q('What does border-radius: 8px do?', ['Rounds all four corners of the element\'s border box by 8 pixels', 'Sets the border width to 8 pixels on all sides', 'Creates an 8-pixel gap between this element and its siblings', 'Rotates the element 8 degrees clockwise'], 0, 'border-radius curves the corners of the border box, creating rounded rectangle shapes.'),
      q('Do horizontal margins collapse between adjacent block elements?', ['No, horizontal margins do not collapse; only vertical margins do', 'Yes, horizontal margins always collapse just like vertical ones', 'Horizontal margins collapse only inside flex containers', 'Horizontal margins collapse only when both elements have the same width'], 0, 'Margin collapse is a vertical-only behavior; left and right margins are always additive.'),
      q('Why do many CSS resets set box-sizing: border-box on all elements?', ['It makes width and height calculations intuitive by including padding and border in the specified dimensions', 'It removes all default browser margins and padding automatically', 'It enables CSS grid layout on every element without extra properties', 'It prevents margin collapse between sibling elements'], 0, 'Global border-box sizing eliminates the common surprise of elements exceeding their specified width due to padding and borders.'),
    ],
  ),
  lesson(
    'CSS Flexbox',
    [
      'Flexbox is a one-dimensional CSS layout model for arranging items in a row or column along a main axis. Apply display: flex to a container element to turn its direct children into flex items. The flex-direction property sets the main axis direction: row (horizontal, default), row-reverse, column (vertical), or column-reverse. This makes Flexbox ideal for navigation bars, card rows, and vertically centered content.',
      'The justify-content property aligns flex items along the main axis. Values include flex-start (default, packed to start), flex-end (packed to end), center (centered), space-between (equal space between items), space-around (equal space around items), and space-evenly (equal space everywhere). Use align-items to align items along the cross axis (perpendicular to the main axis) with values like stretch, flex-start, flex-end, and center.',
      'Flex items can grow, shrink, and have a base size controlled by three properties. flex-grow determines how much extra space an item absorbs (0 means no growth). flex-shrink determines how items compress when space is tight (1 is default). flex-basis sets the initial size before free space is distributed. The shorthand flex: 1 sets flex-grow: 1, flex-shrink: 1, and flex-basis: 0%, making items share space equally.',
      'The flex-wrap property controls whether items stay on one line or wrap to new lines. nowrap (default) keeps all items on a single line, which may cause overflow. wrap allows items to flow onto additional lines, and wrap-reverse wraps in the opposite direction. Combined with flex-direction, wrap enables responsive multi-row layouts without media queries.',
      'Individual flex items can override container alignment with align-self, accepting the same values as align-items but applying to a single item. The order property changes visual order without altering HTML source order—useful for responsive reordering but potentially confusing for screen reader users if misused. The gap property (or row-gap and column-gap) sets spacing between flex items without needing margins.',
      'Common Flexbox patterns include vertically and horizontally centering content (display: flex; justify-content: center; align-items: center), creating equal-width columns (flex: 1 on each child), and building sticky footers where the main content grows to fill available space (flex: 1 on the content area within a column-direction flex container with min-height: 100vh).',
    ],
    [
      q('What CSS property turns an element into a flex container?', ['display: flex', 'position: flex', 'layout: flexbox', 'flex: container'], 0, 'Applying display: flex to a parent element makes its direct children flex items arranged along a flex axis.'),
      q('What does flex-direction: column do?', ['Arranges flex items vertically in a column along the main axis', 'Arranges flex items horizontally in a row along the main axis', 'Reverses the order of items without changing the axis direction', 'Wraps items onto multiple columns automatically'], 0, 'column sets the main axis vertically, stacking items from top to bottom.'),
      q('Which property aligns flex items along the main axis?', ['justify-content', 'align-items', 'align-content', 'flex-wrap'], 0, 'justify-content controls distribution along the main axis; align-items controls the cross axis.'),
      q('What does flex: 1 shorthand set on a flex item?', ['flex-grow: 1, flex-shrink: 1, flex-basis: 0% — making the item grow to fill available space', 'flex-grow: 0, flex-shrink: 0, flex-basis: 100%', 'A fixed width of 1 pixel regardless of container size', 'Automatic centering on both axes within the container'], 0, 'flex: 1 tells the item to grow equally, sharing available space with other flex: 1 siblings.'),
      q('What is the difference between align-items and align-content?', ['align-items aligns items within a single line; align-content aligns multiple lines when flex-wrap is enabled', 'align-items works on rows and align-content works on columns only', 'align-items is for flex containers and align-content is for grid containers', 'align-items and align-content are identical aliases for the same property'], 0, 'align-content only takes effect when there are multiple flex lines due to wrapping; align-items works on each line.'),
      q('What does flex-wrap: wrap allow?', ['Flex items that exceed the container width flow onto additional lines', 'All items to shrink below their minimum content size', 'Items to overlap each other in the same position', 'The container to automatically change from row to column direction'], 0, 'wrap enables multi-line flex layouts, letting items break onto new rows or columns as needed.'),
      q('How does the gap property differ from using margins on flex items?', ['gap sets consistent spacing between items without affecting outer edges or causing margin collapse', 'gap only works in grid layouts, not in flexbox', 'gap adds spacing outside the flex container border', 'gap prevents flex items from growing or shrinking'], 0, 'gap creates uniform spacing between items without the side effects of margin on first/last items or collapse.'),
      q('What does align-self: flex-end do on an individual flex item?', ['Aligns that specific item to the end of the cross axis, overriding the container\'s align-items', 'Moves the item to the end of the main axis regardless of flex-direction', 'Removes the item from the flex container flow entirely', 'Sets the item\'s flex-grow value to zero'], 0, 'align-self overrides the container-level align-items for a single item on the cross axis.'),
      q('Which combination centers content both horizontally and vertically in a flex container?', ['justify-content: center; align-items: center', 'flex-direction: center; flex-wrap: center', 'margin: auto on every flex item', 'text-align: center on the container only'], 0, 'Centering on both axes requires justify-content: center (main axis) and align-items: center (cross axis).'),
      q('Why should the order property be used cautiously with accessibility in mind?', ['Changing visual order without changing DOM order can confuse screen readers that follow the source sequence', 'The order property is not supported in any modern browser', 'The order property permanently reorders HTML in the DOM', 'The order property disables keyboard navigation for reordered items'], 0, 'Screen readers navigate by DOM order, so visual reordering via CSS order can create a mismatch between what is seen and what is heard.'),
    ],
  ),
  lesson(
    'CSS Grid',
    [
      'CSS Grid is a two-dimensional layout system that simultaneously controls rows and columns. Apply display: grid to a container to define a grid formatting context. Unlike Flexbox, which handles one axis at a time, Grid lets you place items precisely in both dimensions using grid-template-columns and grid-template-rows to define track sizes.',
      'Track sizing uses fixed values (px, em), flexible fractions (fr), and intrinsic keywords (auto, min-content, max-content). The declaration grid-template-columns: 1fr 1fr 1fr creates three equal columns, while 200px 1fr 200px creates a flexible center column flanked by fixed sidebars. The repeat() function simplifies repetitive patterns: repeat(3, 1fr) equals 1fr 1fr 1fr.',
      'Grid lines are numbered starting from 1, and items are placed between lines using grid-column and grid-row. The shorthand grid-column: 1 / 3 spans from line 1 to line 3 (covering two columns). grid-area combines row and column placement in one property. Named grid areas, defined in grid-template-areas, enable intuitive layout descriptions like "header header" / "sidebar main" / "footer footer".',
      'The gap property (formerly grid-gap) sets spacing between grid tracks without affecting outer edges. auto-fill and auto-fit keywords in repeat() create responsive grids that automatically add or stretch columns based on available space: repeat(auto-fill, minmax(250px, 1fr)) creates as many 250px-minimum columns as fit.',
      'Grid items can align within their cells using justify-items and align-items on the container (aligning all items) or justify-self and align-self on individual items. justify-content and align-content control the distribution of the grid tracks themselves when the total grid is smaller than its container.',
      'Choose Grid when you need explicit two-dimensional control over rows and columns, such as page layouts, photo galleries, and dashboard interfaces. Combine Grid for the macro page layout with Flexbox for one-dimensional component internals (like a card\'s header row) to leverage the strengths of both systems together.',
    ],
    [
      q('What makes CSS Grid different from Flexbox?', ['Grid is two-dimensional, controlling both rows and columns simultaneously, while Flexbox is one-dimensional', 'Grid only works for text content while Flexbox works for images', 'Grid requires JavaScript to function while Flexbox is pure CSS', 'Grid is deprecated in favor of Flexbox in modern browsers'], 0, 'Grid excels at two-dimensional layouts; Flexbox handles single-axis arrangement of items.'),
      q('What does grid-template-columns: 1fr 2fr 1fr create?', ['Three columns where the center column is twice as wide as each side column', 'Three columns of exactly 1 pixel each', 'One column that spans the full width three times', 'Three rows with fractional heights'], 0, 'fr units distribute available space proportionally; 1fr 2fr 1fr gives the center double the width of each side.'),
      q('What does repeat(4, 1fr) expand to?', ['1fr 1fr 1fr 1fr — four equal fractional columns', '4fr — a single column four times wider than normal', '1fr 4 — one flexible column and one fixed column', 'repeat(1, 4fr) — one column taking all four fractions'], 0, 'repeat(4, 1fr) is shorthand for four equal columns that share available space evenly.'),
      q('How do you make a grid item span two columns starting from the first column line?', ['grid-column: 1 / 3', 'grid-column: span 1', 'grid-row: 1 / 3', 'column-span: 2'], 0, 'grid-column: 1 / 3 places the item from grid line 1 to grid line 3, spanning two column tracks.'),
      q('What does grid-template-areas allow you to do?', ['Define a visual map of named layout regions using ASCII-art-like strings', 'Automatically import area data from a JSON configuration file', 'Create circular grid layouts that wrap around a center point', 'Set the z-index stacking order of grid items'], 0, 'grid-template-areas lets you assign names to regions (e.g., "header header" / "sidebar main") and place items by name.'),
      q('What does repeat(auto-fill, minmax(200px, 1fr)) accomplish?', ['Creates as many columns as fit with a minimum width of 200px, filling available space', 'Creates exactly 200 columns each 1 pixel wide', 'Fills the grid with 200 rows of auto-sized content', 'Repeats the auto-fill keyword 200 times per row'], 0, 'auto-fill with minmax creates a responsive grid that adds columns as space allows, each at least 200px wide.'),
      q('What is the purpose of the gap property in a grid container?', ['It sets uniform spacing between grid rows and columns without affecting the outer container edges', 'It defines the number of columns in the grid', 'It adds padding inside each grid cell around the item content', 'It controls whether grid items can overlap each other'], 0, 'gap (or row-gap/column-gap) creates gutters between tracks, keeping spacing consistent without margin hacks.'),
      q('What is the difference between justify-items and justify-content in Grid?', ['justify-items aligns items within their cells; justify-content aligns the entire grid track structure within the container', 'justify-items works on rows and justify-content works on columns', 'justify-items is for flex containers and justify-content is for grid containers', 'justify-items and justify-content produce identical results in all cases'], 0, 'justify-items controls item placement inside cells; justify-content distributes the grid tracks when they don\'t fill the container.'),
      q('What unit does fr represent in grid track sizing?', ['A fractional unit representing a share of the remaining available space', 'A fixed pixel measurement equal to the root font size', 'A frame-rate unit for animation timing in grid transitions', 'A fraction of the viewport height only'], 0, 'fr divides leftover space proportionally after fixed-size tracks are accounted for.'),
      q('When is combining CSS Grid with Flexbox a good approach?', ['Using Grid for the overall page layout and Flexbox for one-dimensional component internals like nav bars or card headers', 'Never — Grid and Flexbox cannot be used on the same page', 'Only when the page contains exactly twelve grid items', 'Only in browsers that do not support CSS Grid natively'], 0, 'Grid handles macro two-dimensional layouts while Flexbox excels at distributing items along a single axis within components.'),
    ],
  ),
  lesson(
    'Responsive Design',
    [
      'Responsive web design ensures pages look and function well across all screen sizes, from small phones to large desktop monitors. The foundation is a fluid layout that uses relative units (%, em, rem, vw) instead of fixed pixel widths, allowing content to adapt naturally. Combined with the viewport meta tag, this prevents mobile browsers from rendering pages at desktop width and then scaling down.',
      'Media queries apply CSS rules conditionally based on device characteristics. The syntax @media (max-width: 768px) { ... } applies styles when the viewport is 768 pixels or narrower. Common breakpoints target phones (under 576px), tablets (576–768px), small desktops (768–992px), and large screens (992px and above), though breakpoints should be based on content needs rather than specific device models.',
      'Mobile-first design starts with base styles for small screens and uses min-width media queries to add complexity for larger viewports. This approach ensures a functional experience on the smallest devices and progressively enhances for bigger screens. It typically results in smaller CSS files for mobile users since they download only the base styles without unused desktop rules.',
      'Flexible images and media prevent overflow on narrow screens. Setting max-width: 100% and height: auto on images ensures they scale down within their container while maintaining aspect ratio. The HTML picture element with source tags serves different image files at different breakpoints, delivering appropriately sized assets and saving bandwidth on mobile connections.',
      'Responsive typography uses relative units and sometimes clamp() for fluid scaling. The function clamp(1rem, 2.5vw, 2rem) sets a minimum of 1rem, a preferred value of 2.5vw, and a maximum of 2rem, creating text that grows smoothly with viewport width without media query jumps. Line-height and max-width on text blocks improve readability on wide screens.',
      'Testing responsive designs requires more than resizing the browser window. Use DevTools device emulation to test specific screen sizes, touch interaction, and network throttling. Test on real devices when possible, verify that touch targets are at least 44×44 pixels, and ensure no horizontal scrolling occurs at any viewport width.',
    ],
    [
      q('What is the purpose of the viewport meta tag <meta name="viewport" content="width=device-width, initial-scale=1">?', ['It tells mobile browsers to use the device\'s actual width as the layout viewport instead of a simulated desktop width', 'It sets the maximum number of pixels the page can display', 'It disables zooming on all mobile devices permanently', 'It links the page to a responsive CSS framework automatically'], 0, 'Without this meta tag, mobile browsers render at ~980px width and scale down, breaking responsive layouts.'),
      q('What does @media (max-width: 768px) do?', ['Applies the enclosed CSS rules only when the viewport width is 768 pixels or less', 'Applies rules only on devices exactly 768 pixels wide', 'Sets the maximum width of the body element to 768 pixels at all times', 'Hides the page from viewports wider than 768 pixels'], 0, 'max-width media queries activate at and below the specified breakpoint, typically targeting tablets and phones.'),
      q('What is the mobile-first approach to responsive design?', ['Write base CSS for small screens first, then add min-width media queries for larger viewports', 'Design only for mobile devices and ignore desktop users entirely', 'Load a separate website domain for mobile users', 'Use JavaScript to detect mobile devices and redirect to an app store'], 0, 'Mobile-first starts with minimal styles for small screens and progressively enhances with min-width breakpoints.'),
      q('Why should images use max-width: 100% in responsive layouts?', ['It prevents images from overflowing their container on narrow screens while maintaining aspect ratio', 'It forces all images to be exactly 100 pixels wide on every device', 'It converts images to SVG format for faster loading on mobile', 'It hides images entirely on viewports narrower than 480 pixels'], 0, 'max-width: 100% constrains images to their container width; height: auto preserves the aspect ratio.'),
      q('What does the CSS clamp(1rem, 2.5vw, 2rem) function do?', ['Sets a font size that scales with viewport width but stays between 1rem minimum and 2rem maximum', 'Clamps the viewport to a fixed width of 2.5 pixels', 'Restricts the page to displaying only 1 to 2 rem units of content', 'Creates a clamp-shaped border around text elements'], 0, 'clamp(min, preferred, max) provides fluid typography that scales smoothly within defined bounds.'),
      q('What is the advantage of using relative units like rem and % over fixed px for responsive layouts?', ['Relative units scale proportionally with parent or root font size, adapting naturally to different screens', 'Relative units render faster because browsers skip unit conversion', 'Relative units are required for CSS Grid but not for Flexbox', 'Relative units prevent media queries from being needed entirely'], 0, 'Relative units create fluid layouts that adjust without explicit breakpoint rules for every size change.'),
      q('What does the HTML <picture> element with multiple <source> tags enable?', ['Serving different image files at different screen sizes or resolutions for optimal bandwidth usage', 'Displaying multiple images simultaneously in a grid layout', 'Embedding video content that replaces images on hover', 'Creating picture-in-picture overlays for video content'], 0, 'The picture element lets the browser choose the most appropriate image source based on media conditions.'),
      q('Why should responsive breakpoints be based on content rather than specific device widths?', ['Content-driven breakpoints ensure the layout adapts where it actually breaks, not at arbitrary device dimensions', 'Device-specific breakpoints are not supported by any browser', 'Content-driven breakpoints eliminate the need for CSS entirely', 'Device widths never change so content-based breakpoints are unnecessary'], 0, 'New devices constantly emerge with varied screen sizes; content-based breakpoints future-proof the design.'),
      q('What minimum size is recommended for touch targets on mobile interfaces?', ['At least 44×44 CSS pixels to accommodate finger taps accurately', 'At least 10×10 pixels to maximize screen content density', 'Exactly 100×100 pixels on all interactive elements', 'Touch target size does not matter because mobile browsers auto-expand links'], 0, 'Apple and WCAG guidelines recommend minimum 44×44px touch targets to prevent mis-taps and improve usability.'),
      q('Why is testing on real devices important beyond browser DevTools emulation?', ['Real devices reveal actual touch behavior, performance, and rendering quirks that emulation may miss', 'DevTools emulation is not available in any modern browser', 'Real devices automatically fix all CSS layout bugs', 'DevTools can only emulate screen size, not any other device characteristics'], 0, 'Emulation approximates device behavior but cannot fully replicate touch latency, GPU rendering, or network conditions.'),
    ],
  ),
  lesson(
    'JavaScript Basics',
    [
      'JavaScript is the programming language of the web, running in every modern browser and on servers via Node.js. It adds interactivity to pages: responding to user actions, validating forms, fetching data, and updating content dynamically. Include JavaScript with <script src="app.js" defer></script> in HTML, or write it inline—though external files are preferred for maintainability and caching.',
      'Variables store data using let for values that change, const for constants that cannot be reassigned, and var (legacy, function-scoped, generally avoided). JavaScript has dynamic typing: a variable can hold a string, number, boolean, object, array, null, or undefined without explicit type declarations. Template literals using backticks enable embedded expressions: `Hello, ${name}!`.',
      'Core data types include strings ("hello"), numbers (42, 3.14), booleans (true/false), null (intentional absence of value), undefined (uninitialized variable), objects ({ key: "value" }), and arrays ([1, 2, 3]). Arrays support methods like push, pop, map, filter, and reduce for functional data manipulation. Objects store key-value pairs accessed with dot notation (obj.key) or bracket notation (obj["key"]).',
      'Control flow structures direct program execution. if/else statements branch based on conditions. for, while, and for...of loops iterate over sequences. Functions define reusable blocks of code: function greet(name) { return `Hello, ${name}`; }. Arrow functions provide concise syntax: const greet = (name) => `Hello, ${name}`; and inherit this binding from their enclosing scope.',
      'Comparison uses === (strict equality, comparing value and type) rather than == (loose equality, which performs type coercion). The logical operators && (and), || (or), and ! (not) combine conditions. The ternary operator condition ? valueIfTrue : valueIfFalse provides inline conditional expressions.',
      'Modern JavaScript (ES6+) features include destructuring (const { name } = user), spread/rest operators (...items), default parameters, modules (import/export), promises for asynchronous operations, and async/await for writing asynchronous code that reads like synchronous code. Understanding these fundamentals is essential before working with DOM manipulation or frameworks.',
    ],
    [
      q('Which keyword declares a variable whose value cannot be reassigned?', ['const', 'let', 'var', 'static'], 0, 'const creates a constant binding; attempting to reassign it throws a TypeError.'),
      q('What is the difference between === and == in JavaScript?', ['=== compares both value and type strictly; == performs type coercion before comparing', '=== is for strings and == is for numbers only', '=== is deprecated and == is the modern replacement', '=== compares objects and == compares primitives'], 0, 'Strict equality (===) avoids unexpected coercion, e.g., 0 == "" is true but 0 === "" is false.'),
      q('What syntax creates a template literal with an embedded expression?', ['`Hello, ${name}!` using backticks and ${} interpolation', '"Hello, " + name + "!" using double quotes and concatenation', '(\'Hello, \' (name) \'!\') using parentheses', '<<Hello, #{name}!>> using angle brackets'], 0, 'Template literals use backticks and ${expression} to embed variables and expressions in strings.'),
      q('What does the array method .map() do?', ['Creates a new array by applying a function to each element of the original array', 'Removes the last element from the array and returns it', 'Sorts the array elements in alphabetical order permanently', 'Filters out all falsy values from the array'], 0, 'map transforms each element and returns a new array of the same length without mutating the original.'),
      q('How do you define an arrow function assigned to a constant?', ['const add = (a, b) => a + b;', 'function add = (a, b) => a + b;', 'arrow add(a, b) { return a + b; }', 'const add => (a, b) { a + b }'], 0, 'Arrow function syntax is: const name = (params) => expression or { body }.'),
      q('What value does a declared but uninitialized variable hold in JavaScript?', ['undefined', 'null', '0', 'false'], 0, 'Variables declared with let or var but not assigned a value have the undefined type.'),
      q('What does the spread operator ...array do in a function call like Math.max(...numbers)?', ['Expands the array elements as individual arguments to the function', 'Creates a copy of the array nested inside another array', 'Removes duplicate values from the array before passing them', 'Converts the array into a string before passing it'], 0, 'Spread unpacks iterable elements, so Math.max(...[1,5,3]) becomes Math.max(1, 5, 3).'),
      q('What is destructuring in JavaScript?', ['Extracting values from objects or arrays into distinct variables in a single statement', 'Removing all properties from an object to free memory', 'Breaking a string into individual characters permanently', 'Splitting a JavaScript file into multiple module files'], 0, 'Destructuring like const { name, age } = person pulls properties into named variables concisely.'),
      q('What does async/await provide over raw Promise chains?', ['A syntax for writing asynchronous code that reads sequentially, like synchronous code', 'Automatic parallel execution of all async operations simultaneously', 'Complete elimination of network latency in API calls', 'A way to run JavaScript without the event loop'], 0, 'async/await wraps Promises in try/catch-friendly syntax, avoiding deeply nested .then() chains.'),
      q('Why is var generally avoided in modern JavaScript in favor of let and const?', ['var is function-scoped and hoisted, which can cause unexpected behavior; let and const are block-scoped', 'var cannot store string or number values in ES6 and later', 'var is automatically converted to const after the first assignment', 'var prevents the use of arrow functions in the same scope'], 0, 'var\'s function scope and hoisting lead to bugs; let/const respect block scope and have temporal dead zones.'),
    ],
  ),
  lesson(
    'JavaScript DOM',
    [
      'The Document Object Model (DOM) is a tree-shaped representation of an HTML document that JavaScript can read and modify. The browser parses HTML into this tree of nodes, where each element, attribute, and text fragment is a node. The document object is the entry point, and methods like document.querySelector() and document.getElementById() locate specific nodes for manipulation.',
      'Selecting elements uses CSS selectors for flexibility. document.querySelector(".btn") returns the first matching element, while document.querySelectorAll(".btn") returns a NodeList of all matches. Older methods like getElementById, getElementsByClassName, and getElementsByTagName remain valid but are less flexible than querySelector, which accepts any CSS selector syntax.',
      'Reading and modifying content happens through element properties. element.textContent gets or sets plain text (safe from XSS), while element.innerHTML parses and sets HTML markup (use cautiously with user data). Attributes are accessed with getAttribute, setAttribute, and removeAttribute, or directly via properties like element.id, element.className, and element.href for standard attributes.',
      'Creating and inserting elements uses document.createElement("div") to build new nodes, then appendChild, insertBefore, prepend, or append to place them in the tree. element.remove() deletes a node from the DOM. classList provides methods add, remove, toggle, and contains for managing CSS classes without string manipulation of the className property.',
      'Modifying styles directly uses the element.style property: element.style.color = "red" sets an inline style. For reading computed styles (including CSS rules from stylesheets), use window.getComputedStyle(element). Prefer toggling CSS classes over inline styles for maintainability, as classes keep presentation in stylesheets where it belongs.',
      'DOM manipulation triggers reflows and repaints, which are expensive operations. Minimize DOM changes by batching updates, using DocumentFragment to insert multiple elements at once, and avoiding layout thrashing (alternating reads and writes of geometric properties in a loop). Understanding the DOM tree structure is fundamental to all front-end JavaScript work.',
    ],
    [
      q('What is the DOM?', ['A tree-shaped programming interface representing an HTML document that JavaScript can read and modify', 'A CSS preprocessor that compiles stylesheets into optimized binary', 'A network protocol for transferring HTML between servers', 'A JavaScript framework for building single-page applications'], 0, 'The DOM is the browser\'s object representation of the HTML document, enabling programmatic access and modification.'),
      q('What is the difference between querySelector and querySelectorAll?', ['querySelector returns the first matching element; querySelectorAll returns all matches as a NodeList', 'querySelector works only with id selectors; querySelectorAll works only with class selectors', 'querySelector modifies the element; querySelectorAll only reads it', 'querySelector is deprecated; querySelectorAll is the modern replacement'], 0, 'querySelector stops at the first match; querySelectorAll collects every element matching the CSS selector.'),
      q('Why is textContent preferred over innerHTML when setting user-provided data?', ['textContent treats input as plain text, preventing malicious HTML or script injection (XSS)', 'textContent renders HTML formatting like bold and italic tags', 'textContent is faster because it skips the browser\'s HTML parser entirely for all content', 'textContent automatically encrypts the text before displaying it'], 0, 'innerHTML parses strings as HTML, which can execute scripts if user input is inserted unsanitized.'),
      q('How do you add a CSS class to an element without overwriting existing classes?', ['element.classList.add("active")', 'element.className = "active"', 'element.style.class = "active"', 'element.setAttribute("class", "active") with no checking'], 0, 'classList.add appends a class while preserving others; setting className directly replaces all classes.'),
      q('What does document.createElement("li") return?', ['A new <li> element node that exists in memory but is not yet in the document tree', 'A reference to the first existing <li> on the page', 'A string containing the HTML markup "<li></li>"', 'An array of all <li> elements currently in the DOM'], 0, 'createElement builds a new element node that must be inserted into the tree with append or similar methods.'),
      q('What is the purpose of using a DocumentFragment when inserting multiple elements?', ['It allows batching multiple DOM insertions with a single reflow, improving performance', 'It creates a visible container element that wraps the inserted nodes', 'It prevents the inserted elements from being styled by CSS', 'It automatically assigns unique id attributes to each inserted element'], 0, 'DocumentFragment holds nodes off-screen; appending the fragment to the DOM triggers one reflow instead of many.'),
      q('How do you read the final computed value of a CSS property on an element?', ['window.getComputedStyle(element).propertyName', 'element.style.propertyName only', 'document.querySelector(":computed")', 'element.getAttribute("style")'], 0, 'getComputedStyle returns the resolved value after all CSS rules, inline styles, and inheritance are applied.'),
      q('What does element.classList.toggle("open") do?', ['Adds the class "open" if absent, or removes it if present', 'Permanently adds "open" and prevents its removal', 'Checks whether "open" exists but makes no changes', 'Replaces all existing classes with only "open"'], 0, 'toggle switches a class on or off, commonly used for show/hide and menu open/close states.'),
      q('What is layout thrashing in the context of DOM manipulation?', ['Alternating reads and writes of layout properties in a loop, forcing repeated reflows', 'Randomly deleting DOM nodes to test error handling', 'Loading too many external stylesheets simultaneously', 'Using innerHTML instead of textContent for all content updates'], 0, 'Reading offsetHeight after writing styles forces the browser to recalculate layout repeatedly, causing jank.'),
      q('Why is toggling CSS classes generally preferred over setting inline styles with JavaScript?', ['Classes keep presentation in stylesheets, making styles reusable, overridable, and easier to maintain', 'Inline styles set by JavaScript cannot be changed after they are applied', 'CSS classes do not work with JavaScript event listeners', 'Inline styles are not supported in any modern browser'], 0, 'Separating behavior (JS) from presentation (CSS classes) follows best practices and keeps code maintainable.'),
    ],
  ),
  lesson(
    'JavaScript Events',
    [
      'Events are signals that something has happened in the browser: a user clicked, a key was pressed, a form was submitted, or a page finished loading. JavaScript listens for events using addEventListener on a target element: button.addEventListener("click", handleClick). The second argument is a callback function (or object with handleEvent method) invoked when the event fires.',
      'The event object passed to handlers contains useful properties. event.target refers to the element that triggered the event, while event.currentTarget refers to the element the listener is attached to (they differ during event delegation). event.preventDefault() stops the browser\'s default action, such as following a link or submitting a form. event.stopPropagation() prevents the event from bubbling to parent elements.',
      'Event propagation has two phases: capturing (from document down to target) and bubbling (from target back up to document). By default, listeners fire during the bubbling phase. Pass true as the third argument to addEventListener to listen during capturing instead. Most code uses bubbling, which is the default behavior.',
      'Event delegation attaches a single listener to a parent element instead of individual listeners on each child. Because events bubble, clicking a child button inside a <ul> fires the ul\'s listener, where you check event.target to identify which child was clicked. This pattern is efficient for dynamic lists where items are added or removed frequently.',
      'Common event types include click and dblclick for mouse interaction, mouseenter and mouseleave for hover states, keydown and keyup for keyboard input, input and change for form field updates, submit for form submission, load and DOMContentLoaded for page lifecycle, and scroll and resize for viewport changes. Touch events (touchstart, touchmove, touchend) handle mobile interaction.',
      'Remove listeners with removeEventListener when they are no longer needed, such as when a component is destroyed, to prevent memory leaks. Named function references are required for removal—anonymous functions cannot be removed because there is no reference to pass. Debouncing and throttling are techniques to limit how often high-frequency events like scroll or resize trigger expensive handler code.',
    ],
    [
      q('How do you attach a click event listener to a button element in JavaScript?', ['button.addEventListener("click", handleClick)', 'button.onClick = handleClick in HTML only', 'button.event("click", handleClick)', '<button click="handleClick()"> in HTML with no JavaScript'], 0, 'addEventListener is the standard method for registering event handlers on DOM elements.'),
      q('What does event.preventDefault() do inside a form submit handler?', ['Prevents the browser from submitting the form and reloading the page, allowing JavaScript to handle it', 'Removes the form element from the DOM permanently', 'Clears all input fields in the form automatically', 'Prevents other event listeners on the same element from firing'], 0, 'preventDefault stops the default browser action—in this case, the HTTP form submission and page navigation.'),
      q('What is the difference between event.target and event.currentTarget?', ['target is the element that triggered the event; currentTarget is the element the listener is attached to', 'target is always the document root; currentTarget is always the body element', 'target refers to mouse events only; currentTarget refers to keyboard events only', 'target and currentTarget are identical in all situations'], 0, 'During event delegation, target may be a child element while currentTarget is the parent with the listener.'),
      q('What is event bubbling?', ['Events propagate from the target element up through its ancestor elements to the document root', 'Events propagate from the document root down to the target element only', 'Events fire only on the exact element clicked with no propagation', 'Events are duplicated and fired twice on every element in the tree'], 0, 'Bubbling means an event triggered on a nested element also fires listeners on its parent, grandparent, and so on.'),
      q('What is event delegation and why is it useful?', ['Attaching one listener to a parent to handle events from its children, efficient for dynamic content', 'Assigning each event type to a dedicated global handler function', 'Preventing all events from reaching child elements using stopPropagation', 'Converting browser events into custom application-specific events'], 0, 'Delegation leverages bubbling to handle events on current and future child elements with a single parent listener.'),
      q('Which event fires when a user presses a key on the keyboard?', ['keydown', 'click', 'submit', 'load'], 0, 'keydown fires when a key is pressed down; keyup fires when it is released. Both provide event.key for the key value.'),
      q('Why should you use a named function reference when adding event listeners you plan to remove later?', ['removeEventListener requires the same function reference that was passed to addEventListener', 'Anonymous functions execute faster than named functions in all browsers', 'Named functions automatically prevent event bubbling', 'Anonymous functions cannot access the event object parameter'], 0, 'removeEventListener needs an identical function reference; anonymous functions create a new reference each time.'),
      q('What does event.stopPropagation() accomplish?', ['Prevents the event from bubbling up to parent elements\' listeners', 'Stops the browser\'s default action for the event', 'Removes the event listener after it fires once', 'Prevents the event from firing on the target element itself'], 0, 'stopPropagation halts the event\'s upward (or downward) propagation through the DOM tree.'),
      q('When does the DOMContentLoaded event fire?', ['When the HTML document has been fully parsed, before all images and stylesheets finish loading', 'After every resource including images, stylesheets, and fonts has fully loaded', 'Only when the user first clicks anywhere on the page', 'When the browser tab becomes visible after being hidden'], 0, 'DOMContentLoaded fires once the DOM tree is ready, earlier than the load event which waits for all resources.'),
      q('What is debouncing in the context of event handlers?', ['Delaying handler execution until a rapid series of events stops, then running once with the last value', 'Running the handler immediately on every single event with no delay', 'Completely blocking all events from firing on an element', 'Converting mouse events into keyboard events for accessibility'], 0, 'Debouncing waits for a pause in events (like typing or resizing) before executing, reducing unnecessary calls.'),
    ],
  ),
  lesson(
    'Web Accessibility',
    [
      'Web accessibility (a11y) ensures that websites are usable by people with disabilities, including visual, auditory, motor, and cognitive impairments. An estimated one billion people worldwide have some form of disability, and accessible design also benefits users on slow connections, bright sunlight, or mobile devices. Many countries have legal requirements such as the ADA in the United States and EN 301 549 in the European Union.',
      'Semantic HTML is the foundation of accessibility. Using proper heading hierarchy, landmark elements (<main>, <nav>, <aside>), and native form controls provides built-in accessibility without extra effort. Native <button> elements are keyboard-focusable and announce their role to screen readers automatically, unlike <div onclick> which requires manual ARIA attributes and tabindex to become accessible.',
      'ARIA (Accessible Rich Internet Applications) attributes supplement HTML when native semantics are insufficient. Use aria-label to provide an accessible name, aria-describedby to link descriptive text, aria-hidden="true" to hide decorative elements from screen readers, and role to define an element\'s purpose. The first rule of ARIA is: do not use ARIA when a native HTML element already provides the needed semantics.',
      'Keyboard accessibility requires that all interactive elements are reachable and operable via Tab, Enter, Space, and arrow keys. Visible focus indicators (outline styles) show which element is currently focused. Skip links at the top of the page let keyboard users bypass repetitive navigation and jump directly to the main content area.',
      'Color contrast ratios ensure text is readable against its background. WCAG 2.1 Level AA requires at least 4.5:1 for normal text and 3:1 for large text. Never convey information through color alone—pair color changes with icons, text labels, or patterns so users who cannot distinguish colors still understand the message.',
      'Testing accessibility combines automated tools and manual verification. Tools like axe DevTools and Lighthouse catch common issues such as missing alt text and insufficient contrast. Manual testing with keyboard-only navigation and a screen reader (NVDA, VoiceOver) reveals issues that automated tools miss, such as illogical focus order or confusing dynamic content announcements.',
    ],
    [
      q('What does the term "a11y" stand for in web development?', ['Accessibility — the "11" represents the eleven letters between "a" and "y"', 'Automation — eleven automated testing tools combined', 'Alignment — eleven CSS layout properties for grid', 'Analytics — eleven metrics for tracking user behavior'], 0, 'a11y is a numeronym for accessibility, widely used in the web development community.'),
      q('Why is a native <button> element preferred over <div onclick="..."> for interactive controls?', ['Native buttons are automatically keyboard-focusable, announce their role to screen readers, and respond to Enter/Space', 'Native buttons render faster because they skip the CSS box model', 'Native buttons cannot be styled with CSS, ensuring visual consistency', 'Native buttons automatically validate form data before submission'], 0, 'Divs lack built-in semantics and keyboard support; buttons provide these for free with no ARIA needed.'),
      q('What is the first rule of ARIA?', ['Do not use ARIA attributes when a native HTML element already provides the needed semantics', 'Always add role="button" to every clickable element on the page', 'Every element must have at least one aria-label attribute', 'ARIA attributes should replace all semantic HTML elements'], 0, 'Native HTML elements have built-in roles and states; ARIA should only fill gaps where native elements fall short.'),
      q('What WCAG 2.1 Level AA contrast ratio is required for normal-sized text?', ['At least 4.5:1 between text color and background color', 'At least 1.5:1 between text color and background color', 'Exactly 7:1 with no exceptions for any text size', 'Contrast ratios are recommended but not required by WCAG'], 0, 'WCAG AA requires 4.5:1 for normal text and 3:1 for large text (18px+ or 14px+ bold).'),
      q('What is the purpose of a skip link at the top of a web page?', ['It allows keyboard users to bypass repetitive navigation and jump directly to the main content', 'It skips loading CSS files to improve page performance', 'It skips JavaScript execution until the user clicks the page', 'It provides a link to skip to the next page in a multi-page form'], 0, 'Skip links (e.g., "Skip to main content") save keyboard users from tabbing through every nav item on every page.'),
      q('What does aria-describedby do?', ['Links an element to another element whose text provides additional description, read by screen readers after the name', 'Hides the described element from all users including sighted users', 'Automatically generates a description based on the element\'s CSS classes', 'Describes the element\'s visual appearance including color and font size'], 0, 'aria-describedby references an element id whose content supplements the accessible name with extra context.'),
      q('Why should information never be conveyed through color alone?', ['Users with color blindness or who view screens in bright light may not distinguish color differences', 'Colors are not supported by HTML and require JavaScript to display', 'Search engines cannot index colored text properly', 'CSS color properties are deprecated in favor of aria-color attributes'], 0, 'Relying solely on color excludes users with visual impairments; always add text, icons, or patterns as redundant cues.'),
      q('What keyboard keys should all interactive elements respond to for basic accessibility?', ['Tab to reach the element, Enter or Space to activate it', 'Only mouse clicks; keyboard interaction is optional for accessibility', 'Escape to activate and Tab to deactivate every element', 'Arrow keys only, with no Tab or Enter support needed'], 0, 'Keyboard users navigate with Tab and activate buttons/links with Enter or Space—this must work on all interactive controls.'),
      q('What is the role of alt text on an <img> element?', ['It provides a text description of the image for screen readers and when the image fails to load', 'It sets the alternative CSS filter to apply when the image is hovered', 'It links the image to an alternative high-resolution version for retina displays', 'It prevents the image from being indexed by search engines'], 0, 'alt text conveys image content to assistive technologies; decorative images should use alt="" to be ignored.'),
      q('Why is manual testing with a screen reader important beyond automated accessibility tools?', ['Automated tools catch roughly 30-40% of issues; manual testing reveals focus order, context, and dynamic content problems', 'Automated tools are not available for any website or browser', 'Screen readers automatically fix all accessibility issues they detect', 'Automated tools only work on pages written in React or Vue frameworks'], 0, 'Tools like axe catch missing attributes and contrast issues but miss illogical reading order and confusing live region updates.'),
    ],
  ),
  lesson(
    'Performance Basics',
    [
      'Web performance directly affects user experience, conversion rates, and search engine rankings. Google\'s Core Web Vitals measure three key metrics: Largest Contentful Paint (LCP, loading speed, target under 2.5s), Interaction to Next Paint (INP, responsiveness, target under 200ms), and Cumulative Layout Shift (CLS, visual stability, target under 0.1). Monitoring these metrics guides optimization priorities.',
      'Minimize the critical rendering path by reducing the number and size of resources the browser must download and parse before displaying content. Place CSS in the head so styles arrive early, defer non-critical JavaScript with the defer attribute, and eliminate render-blocking resources where possible. Inline critical above-the-fold CSS for the fastest first paint.',
      'Image optimization has outsized impact because images typically account for the largest portion of page weight. Use modern formats like WebP and AVIF that offer better compression than JPEG and PNG. Serve appropriately sized images with srcset and sizes attributes so mobile devices download smaller files. Lazy-load below-the-fold images with loading="lazy" to defer off-screen downloads.',
      'Minify and compress text assets: minification removes whitespace and comments from CSS and JavaScript files, while gzip or Brotli compression reduces transfer size by 60-80%. Enable compression on the web server and use build tools (esbuild, Terser, cssnano) to minify assets during deployment. Concatenating many small files into fewer requests also reduces HTTP overhead.',
      'Caching strategies reduce repeat load times. Set Cache-Control headers to tell browsers and CDNs how long to store assets. Use content hashing in filenames (app.a1b2c3.js) for long-term caching of static assets, since the hash changes only when content changes. HTML files typically use shorter cache durations or no-cache to ensure users receive updates.',
      'Measure before optimizing using Lighthouse in Chrome DevTools, WebPageTest, or real-user monitoring tools. Profile JavaScript execution in the Performance panel to find long tasks blocking the main thread. Avoid premature optimization—identify the actual bottlenecks through measurement, then apply targeted fixes rather than applying every technique indiscriminately.',
    ],
    [
      q('What does Largest Contentful Paint (LCP) measure?', ['How long it takes for the largest visible content element to render on screen', 'The total number of HTTP requests made during page load', 'The amount of JavaScript memory consumed by the page', 'The time until the user first clicks any interactive element'], 0, 'LCP tracks when the main content (hero image, heading block) becomes visible—target is under 2.5 seconds.'),
      q('What is Cumulative Layout Shift (CLS)?', ['A metric measuring unexpected visual movement of page elements during loading', 'The total number of CSS rules applied to the page', 'The amount of horizontal scrolling required on mobile devices', 'The time between a click and the browser\'s response'], 0, 'CLS quantifies visual instability—elements shifting position unexpectedly—which frustrates users trying to click or read.'),
      q('Why should JavaScript files use the defer attribute?', ['defer downloads scripts in parallel but executes them after HTML parsing completes, avoiding render blocking', 'defer prevents JavaScript from ever executing on the page', 'defer converts JavaScript files into CSS automatically', 'defer loads scripts only when the user scrolls to the bottom of the page'], 0, 'Deferred scripts don\'t block HTML parsing, improving initial render time while still executing in order before DOMContentLoaded.'),
      q('What advantage do WebP and AVIF image formats offer over JPEG and PNG?', ['Better compression at equivalent visual quality, resulting in smaller file sizes and faster downloads', 'They automatically resize to fit any container without CSS', 'They are the only formats supported by HTML5 img elements', 'They load images progressively from top to bottom by default'], 0, 'Modern formats like WebP and AVIF achieve 25-50% smaller files than JPEG at similar quality, reducing page weight.'),
      q('What does loading="lazy" on an <img> element do?', ['Defers downloading the image until it is near the viewport, saving bandwidth for below-the-fold content', 'Loads the image at a lower resolution first and upgrades on click', 'Prevents the image from being displayed until JavaScript enables it', 'Downloads the image twice for redundancy and reliability'], 0, 'Native lazy loading postpones off-screen image downloads until the user scrolls near them.'),
      q('Why do build tools add content hashes to filenames like app.a1b2c3.js?', ['The hash changes when content changes, enabling long-term browser caching without serving stale files', 'Hashes encrypt the file contents to prevent unauthorized access', 'Hashes are required by HTTP/2 to multiplex file transfers', 'Hashes automatically minify the file during the naming process'], 0, 'Hashed filenames allow Cache-Control: max-age=31536000 because a content change produces a new filename.'),
      q('What is the critical rendering path?', ['The sequence of steps the browser takes to convert HTML, CSS, and JS into rendered pixels on screen', 'The file path to the most important CSS stylesheet on the server', 'The order in which JavaScript functions are defined in a file', 'The network route with the lowest latency to the web server'], 0, 'Optimizing the critical path means minimizing resources and processing needed before first paint.'),
      q('What does minification do to CSS and JavaScript files?', ['Removes whitespace, comments, and shortens identifiers to reduce file size without changing functionality', 'Combines all CSS and JavaScript into a single HTML file', 'Converts JavaScript to WebAssembly for faster execution', 'Removes all unused CSS selectors and JavaScript functions automatically'], 0, 'Minification shrinks source files for faster transfer; tree-shaking (separate step) removes unused code.'),
      q('What is the purpose of srcset on an <img> element?', ['It provides multiple image source options at different resolutions so the browser picks the best fit', 'It sets the source code repository URL for the image asset', 'It defines a set of CSS styles to apply to the image at different breakpoints', 'It creates a slideshow by cycling through multiple images automatically'], 0, 'srcset with sizes lets the browser download an appropriately sized image for the device\'s screen density and viewport.'),
      q('Why should you measure performance before applying optimizations?', ['Measurement identifies actual bottlenecks so you fix what matters instead of wasting effort on non-issues', 'Browsers refuse to apply optimizations unless Lighthouse scores are recorded first', 'Measurement is only needed once at project launch, never again', 'Optimizations cannot be applied without a paid performance monitoring subscription'], 0, 'Profiling reveals whether images, JavaScript, fonts, or network latency is the real problem—each needs different fixes.'),
    ],
  ),
  lesson(
    'Version Control Intro',
    [
      'Version control systems track changes to files over time, enabling developers to review history, revert mistakes, and collaborate without overwriting each other\'s work. Git is the dominant version control system in web development, used locally and hosted on platforms like GitHub, GitLab, and Bitbucket. Every project should use version control from its first commit.',
      'A Git repository contains the full history of a project. The working directory holds your current files, the staging area (index) holds changes prepared for the next commit, and the repository stores all committed snapshots. The command git add stages changes, git commit saves a snapshot with a descriptive message, and git status shows what has changed.',
      'Commits should be atomic (one logical change per commit) with clear messages explaining why the change was made. Good messages follow a pattern like "fix: prevent form submission on empty email field" rather than vague text like "updates" or "fix bug." Conventional prefixes like feat, fix, docs, and refactor help teams scan history quickly.',
      'Branches allow parallel development without affecting the main codebase. Create a branch with git checkout -b feature/contact-form, make commits on it, then merge back into main when the feature is complete. The main (or master) branch typically holds production-ready code, while feature branches isolate work in progress.',
      'Remote repositories on GitHub enable team collaboration. git remote add origin <url> links a local repo to a remote, git push uploads commits, and git pull downloads and merges remote changes. Pull requests (or merge requests) provide a review workflow where teammates examine code changes, run automated tests, and discuss improvements before merging.',
      'Essential Git commands for daily web development include git log (view history), git diff (see unstaged changes), git stash (temporarily save uncommitted work), git revert (undo a commit with a new commit), and git clone (copy a remote repository locally). Understanding these commands prevents data loss and enables confident collaboration on team projects.',
    ],
    [
      q('What is the primary purpose of a version control system like Git?', ['To track file changes over time, enabling history review, collaboration, and safe rollback', 'To compile JavaScript and CSS into optimized production bundles', 'To host websites on the internet for public access', 'To automatically test HTML for validation errors on every save'], 0, 'Version control records every change, letting teams collaborate and recover previous states safely.'),
      q('What does git add do?', ['Stages changed files in the index, preparing them for the next commit', 'Immediately uploads files to the remote repository on GitHub', 'Creates a new branch for the changed files', 'Permanently deletes untracked files from the working directory'], 0, 'git add moves changes from the working directory to the staging area; git commit then saves the staged snapshot.'),
      q('What makes a good Git commit message?', ['A clear description of why the change was made, such as "fix: validate email before form submission"', 'A single word like "update" or "changes" to keep messages short', 'The date and time the commit was created with no other context', 'A list of every file that was modified without explaining the purpose'], 0, 'Good messages explain the intent behind the change, helping teammates understand history without reading diffs.'),
      q('Why are branches used in Git workflows?', ['They allow developers to work on features or fixes in isolation without affecting the main codebase', 'They permanently split the project into separate unrelated repositories', 'They automatically deploy code to production servers', 'They compress the repository to save disk space on the local machine'], 0, 'Branches isolate work so multiple developers can progress in parallel and merge when ready.'),
      q('What is the difference between git push and git pull?', ['push uploads local commits to the remote; pull downloads and merges remote changes into your local branch', 'push creates a new branch; pull deletes an existing branch', 'push is for HTML files and pull is for CSS files only', 'push and pull are identical commands with different names'], 0, 'push sends your commits upstream; pull fetches remote changes and integrates them into your current branch.'),
      q('What is a pull request on GitHub?', ['A proposal to merge a branch into another, enabling code review and discussion before integration', 'A request to download the entire repository as a ZIP file', 'A command that forces Git to overwrite local changes with remote ones', 'An automated email sent whenever any file in the repository changes'], 0, 'Pull requests create a review checkpoint where teammates examine, comment on, and approve changes before merging.'),
      q('What does git status show?', ['Which files are modified, staged, or untracked in the working directory compared to the last commit', 'The complete source code of every file in the repository', 'A list of all developers who have ever contributed to the project', 'The network speed between your machine and the remote repository'], 0, 'git status is the first command to run when checking what has changed and what is ready to commit.'),
      q('What does git stash do?', ['Temporarily saves uncommitted changes so you can switch branches with a clean working directory', 'Permanently deletes all uncommitted changes without any recovery option', 'Creates a backup copy of the entire repository on an external drive', 'Automatically commits all changes with a default message'], 0, 'stash lets you shelve work-in-progress, switch context, and later restore the changes with git stash pop.'),
      q('Why should commits be atomic (one logical change per commit)?', ['It makes history easy to review, revert, and bisect because each commit represents a single coherent change', 'Git requires exactly one file change per commit by design', 'Atomic commits automatically run all project tests before saving', 'Non-atomic commits corrupt the Git repository database'], 0, 'Atomic commits allow reverting a specific fix without undoing unrelated changes bundled in the same commit.'),
      q('What does git clone do?', ['Creates a local copy of a remote repository, including its full commit history', 'Creates an empty Git repository with no files or history', 'Clones a website\'s visual design into a CSS stylesheet', 'Duplicates a single file within the same repository'], 0, 'git clone downloads the entire repository from a remote URL, setting up a local working copy with history.'),
    ],
  ),
  lesson(
    'Deployment Overview',
    [
      'Deployment is the process of making a web application available to users on the internet. For static sites (HTML, CSS, JavaScript files), deployment means uploading files to a hosting service. For dynamic applications, it also involves configuring servers, databases, environment variables, and domain names. The goal is a reliable, secure, and performant production environment.',
      'Static site hosting platforms like Netlify, Vercel, GitHub Pages, and Cloudflare Pages offer free tiers suitable for learning and portfolio projects. Connect a Git repository, and these platforms automatically build and deploy on every push to the main branch. They provide HTTPS certificates, global CDN distribution, and preview deployments for pull requests.',
      'The typical deployment workflow connects Git to the hosting platform. Push code to GitHub, the platform detects the push via webhook, runs a build command if needed (npm run build), and publishes the output directory (dist, build, or public) to its CDN. Failed builds block deployment, preventing broken code from reaching users.',
      'Environment variables store sensitive configuration like API keys, database URLs, and secret tokens outside of source code. Set them in the hosting platform\'s dashboard rather than committing them to Git. A .env file loaded locally should be listed in .gitignore to prevent accidental exposure. Production, staging, and development environments each have their own variable values.',
      'Custom domains connect a memorable name like myportfolio.com to the hosting platform\'s servers. Configure DNS records (typically a CNAME or A record) at your domain registrar pointing to the hosting provider. Most platforms automatically provision and renew TLS certificates, ensuring HTTPS is enabled without manual certificate management.',
      'Post-deployment practices include monitoring uptime with services like UptimeRobot, checking analytics for traffic patterns, setting up error tracking with Sentry, and planning a rollback strategy if a deployment introduces bugs. Preview deployments let you test changes on a unique URL before merging to production, reducing the risk of shipping broken features to real users.',
    ],
    [
      q('What is web deployment?', ['The process of making a web application accessible to users on the internet in a production environment', 'The process of writing HTML and CSS code on a local development machine', 'The process of converting JavaScript to TypeScript before coding begins', 'The process of registering a domain name with a DNS provider only'], 0, 'Deployment publishes your application from development to a live server where users can access it.'),
      q('What do static site hosting platforms like Netlify and Vercel provide automatically?', ['HTTPS certificates, global CDN distribution, and deployment on every Git push', 'A built-in database server and user authentication system for every project', 'Automatic conversion of HTML pages into native mobile apps', 'Unlimited server-side CPU for running complex backend computations'], 0, 'These platforms handle SSL, CDN caching, and continuous deployment from Git with minimal configuration.'),
      q('Why should API keys and secret tokens be stored as environment variables rather than in source code?', ['They keep sensitive credentials out of the Git repository, preventing accidental public exposure', 'Environment variables make the application run faster by caching credentials in memory', 'Source code cannot read string values at runtime without environment variables', 'Git automatically deletes any file that contains the word "secret" or "key"'], 0, 'Committing secrets to Git exposes them in history permanently; environment variables keep them out of code.'),
      q('What should a .gitignore file contain regarding environment configuration?', ['The .env file (and similar secret-containing files) to prevent them from being committed to Git', 'All HTML and CSS files since they are generated automatically', 'The package.json file to prevent dependency information from leaking', 'Every JavaScript file in the project for security purposes'], 0, '.gitignore excludes specified files from Git tracking; .env files with secrets must always be ignored.'),
      q('How do you connect a custom domain to a hosting platform?', ['Configure DNS records (CNAME or A record) at your domain registrar pointing to the hosting provider', 'Rename the hosting platform\'s server to match your domain name', 'Add the domain name as a comment in the HTML head section', 'Purchase the domain from the same company that wrote the HTML code'], 0, 'DNS records at your registrar tell browsers where to find your site when they look up your custom domain.'),
      q('What is a preview deployment?', ['A temporary deployment of a branch or pull request on a unique URL for testing before merging to production', 'A low-resolution version of the site shown only to search engine crawlers', 'A deployment that automatically deletes itself after 24 hours with no option to extend', 'A preview of the site\'s design mockup that is not functional code'], 0, 'Preview deployments create a live test URL for each branch/PR, enabling review before production release.'),
      q('What happens when a platform\'s build step fails during deployment?', ['The deployment is blocked and the previous successful version remains live for users', 'The broken code is deployed anyway and users see error pages', 'The platform automatically rewrites the code to fix build errors', 'All previous deployments are deleted from the CDN permanently'], 0, 'Failed builds prevent broken code from reaching production; the last good deployment continues serving users.'),
      q('What is the purpose of a CDN (Content Delivery Network) in deployment?', ['It caches and serves static assets from servers geographically close to users, reducing load times', 'It compiles JavaScript source code into optimized machine code at the edge', 'It replaces the need for HTML files by generating pages on demand from API data', 'It provides free domain name registration for all deployed projects'], 0, 'CDNs replicate assets across global edge servers, so users download from a nearby location instead of a distant origin.'),
      q('Why is having separate production, staging, and development environments important?', ['Each environment can have its own configuration and data, preventing test changes from affecting real users', 'Browsers require three environments to render CSS correctly', 'Git cannot function with fewer than three separate repositories', 'Search engines index only the staging environment by default'], 0, 'Separate environments isolate testing from live users; staging mirrors production for final verification.'),
      q('What is a rollback strategy in deployment?', ['A plan to quickly revert to a previous working version if a new deployment introduces critical bugs', 'A strategy for rolling back Git commits by deleting the entire repository', 'A method for converting a dynamic site back into static HTML files', 'A technique for undoing user registrations on a live website'], 0, 'Rollback plans—redeploying a previous commit or using platform rollback features—minimize downtime when deployments fail.'),
    ],
  ),
]
